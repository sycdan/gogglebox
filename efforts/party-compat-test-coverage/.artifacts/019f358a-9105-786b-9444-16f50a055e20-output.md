---
output_id: 019f358a-9105-786b-9444-16f50a055e20
prompt_id: 019f358a-723d-74f0-b9c0-4afd9c223b2a
agent: gogglebox-verifier
effort_path: efforts/party-compat-test-coverage/PartyCompatTestCoverage.md
---

# Party Compat Test Coverage — independent verification

## Status: PASS

## Commands run (fresh, from this effort's own worktree)

| Command | Exit code | Notes |
|---|---|---|
| `docker compose run --rm check` | 0 | `tsc -p tsconfig.json --noEmit && tsc -p tsconfig.server.json --noEmit`, zero errors |
| `docker compose run --rm test` | 0 | `# tests 156`, `# pass 152`, `# fail 0`, `# cancelled 0`, `# skipped 4` |

156 total = the prior 150-test baseline (146 pass / 4 pre-existing
real-Jellyfin skips) + 6 new tests, all passing (1 golden-value in
`partyKey.test.ts` + 5 HTTP-route-level in the new `server.test.ts`). No
regression; only additions. Re-ran `test` twice, identical result both times.

## AC1 — golden-value regression test (`src/server/partyKey.test.ts`)

Confirmed the new test (`derivePartyKey golden values match pre-computed hex
strings`) asserts three hard-coded 32-char hex literals against
`derivePartyKey(...)` output — not a self-referential re-derive (unlike the
five pre-existing tests in the same file, which all compare one
`derivePartyKey(...)` call against another and would pass under any
internally-consistent namespace change).

Independently re-derived the same UUIDv5 algorithm from `src/server/partyKey.ts`
in a standalone script and got byte-identical output to all three literals
in the test:

```
dd73cc99d93b5b219b5a520361778e43
f4f932ec88cf54989bd67d1a27eff523
8fb7b620553c52a3ac6f772fb33751ab
```

This confirms the literals are correctly derived from the current
`PARTY_NAMESPACE`, not typo'd or copied from a different source.

**Gap flagged (not a defect):** the builder's mutate-`PARTY_NAMESPACE`-and-
confirm-single-failure check (documented in
`efforts/party-compat-test-coverage/.artifacts/019f3571-ef9d-7b28-84eb-aed745f11112-output.md`)
was not independently re-executed by this verifier. An attempt to temporarily
edit `src/server/partyKey.ts` (with immediate revert) to reproduce this check
was explicitly blocked by this environment's tool-use policy, which treats
any edit to a tracked `src/` file as disallowed for a read-only verifier
regardless of intended revert. This is a verification-process gap, not a
finding against the implementation — the algorithm's structure (SHA-1 over
namespace bytes + name) guarantees a namespace change alters every derived
hash, and the independent re-derivation above corroborates the golden values
are namespace-sensitive and correct as committed.

Proof: `efforts/party-compat-test-coverage/.artifacts/019f3495-1612-71cf-8d95-2b9d125396e1-proof.md`

## AC2 — HTTP-route-level alias parity test (`src/server/server.test.ts`)

Confirmed the new file drives a real `node:http` listener
(`http.createServer(app).listen(0, '127.0.0.1', ...)`, `app` built via
`createApp(config, jellyfin, appState)`) and exercises, via global `fetch`
with a real login + session-cookie round trip, all five required alias pairs
with `assert.deepEqual` body comparisons:

- `POST /api/party` vs `POST /api/group` (create)
- `POST /api/party/verify-pins` vs `POST /api/group/verify-pins`
- `GET /api/parties` vs `GET /api/groups`, including per-entry
  `partyKey`/`groupKey` value equality
- `POST /api/party/clear` vs `POST /api/group/clear`
- `GET /api/session`'s `activePartyAlias` vs `activeGroupAlias`

No live Jellyfin call is exercised (party pre-seeded into `appState` in every
test so `handleCreateParty`'s Jellyfin-calling branch is never taken).
Approach (plain `node:test` + `node:http` + `fetch`, no supertest) matches the
Goals/Nongoals preference and the repo's existing `src/server/*.test.ts`
conventions.

Proof: `efforts/party-compat-test-coverage/.artifacts/019f3495-3105-7457-bb87-42d041ade278-proof.md`

## `createApp()` extraction review (`src/server/server.ts`)

Ran `git diff --ignore-all-space` between the commit preceding this effort's
changes (`74de714`) and the builder's landed commit (`6a56731`) on
`src/server/server.ts`. The entire diff is exactly:

1. An `export function createApp(config, jellyfin, appState):
   express.Express { ... return app; }` wrapper inserted around the
   pre-existing route-registration body (all route handlers and route-scoped
   helpers moved inside unchanged — zero semantic diff beyond added
   indentation).
2. The module-level call site changed from `const app = express();` to
   `const app = createApp(config, jellyfin, appState);` — still called
   unconditionally, once, at module scope, with the real production
   `config`/`jellyfin`/`appState` objects.
3. `const isEntryPoint = require.main === module;` wrapping the pre-existing
   startup IIFE (live `jellyfin.fetchUsers()`, effective-config build/cache,
   `app.listen(config.port, ...)`) in `if (isEntryPoint) { ... }`.
   `isEntryPoint` is `true` for every real invocation (`npm start`,
   `dev:server`, Docker `CMD` all run `server.ts`/`server.js` directly as the
   Node entrypoint) and only `false` when the module is `import()`ed as a
   library, which only happens in the new `server.test.ts`.

No route, handler, response shape, middleware order, config-loading
behavior, or Jellyfin-connectivity requirement changed for the production
entrypoint. This confirms the extraction is additive-only as the effort spec
requires.

## Overall verdict

PASS — all three acceptance criteria are satisfied. One process gap is
flagged above (AC1's mutate-and-fail re-execution could not be independently
reproduced due to this verifier's read-only-on-`src/` sandboxing), but it does
not indicate any defect in the delivered implementation; the golden-value
literals were independently confirmed correct by re-deriving them from the
current source.

## Relevant file paths

- `C:\Users\Dan\Projects\gogglebox\sessions\party-compat-test-coverage.1783309702317\src\server\partyKey.test.ts`
- `C:\Users\Dan\Projects\gogglebox\sessions\party-compat-test-coverage.1783309702317\src\server\partyKey.ts`
- `C:\Users\Dan\Projects\gogglebox\sessions\party-compat-test-coverage.1783309702317\src\server\server.test.ts`
- `C:\Users\Dan\Projects\gogglebox\sessions\party-compat-test-coverage.1783309702317\src\server\server.ts`
- `C:\Users\Dan\Projects\gogglebox\sessions\party-compat-test-coverage.1783309702317\efforts\party-compat-test-coverage\.proofs\019f3495-1612-71cf-8d95-2b9d125396e1.md`
- `C:\Users\Dan\Projects\gogglebox\sessions\party-compat-test-coverage.1783309702317\efforts\party-compat-test-coverage\.proofs\019f3495-3105-7457-bb87-42d041ade278.md`
- `C:\Users\Dan\Projects\gogglebox\sessions\party-compat-test-coverage.1783309702317\efforts\party-compat-test-coverage\.proofs\019f3495-4bd6-7296-a123-1c219ddab732.md`
