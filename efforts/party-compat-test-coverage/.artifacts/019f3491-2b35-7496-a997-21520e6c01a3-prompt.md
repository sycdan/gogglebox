---
prompt_id: 019f3491-2b35-7496-a997-21520e6c01a3
target_agent: gogglebox-planner
effort_path: efforts/party-compat-test-coverage/PartyCompatTestCoverage.md
output_path: efforts/party-compat-test-coverage/.artifacts/019f3491-46fe-7f5d-bcab-76bb09d7af64-output.md
base_tag: handoff/019f3491-2b35-7496-a997-21520e6c01a3
session_name: party-compat-test-coverage.1783293364635
---

First, `cd` into `./sessions/party-compat-test-coverage.1783293364635` and do
all edits from there.

## Context

Replace the stub at
`efforts/party-compat-test-coverage/PartyCompatTestCoverage.md` with a
complete effort spec (Overview, Goals, Nongoals, ordered Acceptance Criteria
with seeded UUIDv7 proof links, per
`kb/00000000-0000-0000-0000-000000000000.md`).

This effort originates from two non-blocking gaps flagged during the now-done
[rename-group-to-party](../rename-group-to-party/RenameGroupToParty.md)
effort's verification and approval phases (already cited in the stub):

1. `src/server/partyKey.ts` has a `PARTY_NAMESPACE` UUIDv5 namespace constant
   explicitly commented as load-bearing/must-never-change (it seeds every
   existing deployment's persisted party keys and derived Jellyfin
   usernames), but `src/server/partyKey.test.ts` only asserts internal
   self-consistency (determinism, order-independence) — no golden/pinned
   hash value. An accidental future change to that constant would pass all
   existing tests silently.
2. There is no HTTP-route-level (supertest-style) test anywhere in
   `src/server/` proving the `/api/group*` compatibility aliases in
   `src/server/server.ts` stay wired to the same handlers as `/api/party*`
   with identical response bodies (including the dual `activePartyAlias`/
   `activeGroupAlias` and `parties`/`groups` fields). This currently rests
   only on manual curl checks against a live sandbox done during that
   effort's build/proof phases, not on CI-enforced automation. Note this is
   the *first* HTTP-route-level test in the repo (there is no existing
   `server.test.ts`/supertest usage at all) — the planner should scope
   whether to introduce a minimal supertest harness for just these routes or
   find an alternative that fits the existing test conventions in
   `src/server/*.test.ts`.

Read `efforts/rename-group-to-party/.artifacts/019f33e2-5c9b-73fc-8082-3ec70b50e197-output.md`
and `efforts/rename-group-to-party/.artifacts/019f3452-31cb-7fcd-86a4-09afea62bbaf-output.md`
for full original context on both gaps before scoping acceptance criteria.

You may only edit under `./efforts`. Write your final summary (the completed
spec's shape, any open questions for the builder) to
`efforts/party-compat-test-coverage/.artifacts/019f3491-46fe-7f5d-bcab-76bb09d7af64-output.md`.
