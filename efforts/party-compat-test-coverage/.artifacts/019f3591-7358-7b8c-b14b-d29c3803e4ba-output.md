---
output_id: 019f3591-7358-7b8c-b14b-d29c3803e4ba
prompt_id: 019f3591-58d9-7ae0-a54d-710bf8f7411b
agent: gogglebox-approver
effort_path: efforts/party-compat-test-coverage/PartyCompatTestCoverage.md
---

# Party Compat Test Coverage — approval

## Status: pass

All 3 acceptance criteria checked in
`efforts/party-compat-test-coverage/PartyCompatTestCoverage.md`. The effort is
done; no subefforts, no parent effort.

## Checked

1. **AC1 — golden-value regression test for `derivePartyKey`.** Checked.
   Read `src/server/partyKey.test.ts` directly in this worktree and confirmed
   the test `derivePartyKey golden values match pre-computed hex strings`
   (lines 40-47) exists exactly as both the builder's output and the
   verifier's proof describe: three hard-coded 32-char hex literals
   (`dd73cc99d93b5b219b5a520361778e43`, `f4f932ec88cf54989bd67d1a27eff523`,
   `8fb7b620553c52a3ac6f772fb33751ab`) asserted against live
   `derivePartyKey(...)` output for three fixed viewer-id inputs — not a
   self-referential re-derive (contrast with the five pre-existing tests in
   the same file). Read `src/server/partyKey.ts` and confirmed the
   `PARTY_NAMESPACE` constant and UUIDv5 algorithm (SHA-1 over namespace
   bytes + sorted/joined name, version/variant bit patching, dash-stripped
   32-char output) match exactly what both the builder and the independent
   verifier described when they separately reimplemented the algorithm
   standalone and reproduced all three literals byte-for-byte.

   On the flagged process gap (verifier could not independently re-run the
   builder's mutate-`PARTY_NAMESPACE`-and-observe-single-failure check,
   being read-only on `src/`): judged adequate, not blocking. The criterion
   text requires "the proof demonstrates" the mutate/fail behavior — the
   builder's own output
   (`.artifacts/019f3571-ef9d-7b28-84eb-aed745f11112-output.md`) documents having
   executed this twice (temporarily changed the namespace's last two hex
   digits, ran `docker compose run --rm test`, observed exactly one failure
   — the new golden test — with all five pre-existing self-consistency
   tests still green, then reverted). That is direct executed evidence, not
   a bare chat assertion; it's recorded in a proof-adjacent output document
   with specific observed test names/counts. The verifier's inability to
   redo the same mutation independently is a sandboxing limitation on the
   verifier's own environment, not a defect or gap in the underlying
   evidence, and the verifier substituted a separately sufficient
   check — independently reimplementing the algorithm and matching the
   golden values byte-for-byte — which rules out the golden values being
   arbitrary/typo'd/vacuous. Combined with the algorithm's structural
   property (SHA-1 avalanche: any namespace-byte change necessarily changes
   every derived hash, which cannot leave the golden-comparison unaffected
   while leaving order-independence/dedup/format/determinism checks
   unaffected), this is sufficient to check AC1.

2. **AC2 — HTTP-route-level alias parity test.** Checked. Read
   `src/server/server.test.ts` in full (316 lines) directly in this
   worktree. Confirmed it drives a real `node:http` listener
   (`http.createServer(app).listen(0, '127.0.0.1', ...)` against an `app`
   built by `createApp(config, jellyfin, appState)`), uses global `fetch`
   with a real `POST /api/auth/login` + manually-carried session cookie
   (not a bypassed/mocked auth path), and covers all five required pairs
   with `assert.deepEqual` body comparisons: `/api/party` vs `/api/group`
   (create), `/api/party/verify-pins` vs `/api/group/verify-pins`,
   `/api/parties` vs `/api/groups` (plus explicit
   `partiesBody.groups[0].groupKey === partiesBody.parties[0].partyKey`
   per-entry check), `/api/party/clear` vs `/api/group/clear`, and
   `GET /api/session`'s `activePartyAlias`/`activeGroupAlias` equality.
   Read `src/server/server.ts` and confirmed the `createApp()` seam
   (line 247), the unconditional production call site
   `const app = createApp(config, jellyfin, appState);` (line 976), and the
   `isEntryPoint`/`require.main === module` guard (lines 1062-1064) wrapping
   the pre-existing live-Jellyfin-fetch + `app.listen(...)` startup IIFE —
   matching exactly what both the builder's output and the verifier's proof
   describe as an additive-only extraction with no production-path change.
   Approach matches the Goals/Nongoals preference (plain `node:test` +
   `node:http` + `fetch`, no supertest) and fits the existing
   `src/server/*.test.ts` conventions.

3. **AC3 — fresh green `check`/`test`, no regression.** Checked. The
   proof doc and both supporting outputs (builder's and independent
   verifier's) report identical, consistent results: `docker compose run
   --rm check` exit 0 with zero typecheck errors, and `docker compose run
   --rm test` producing `# tests 156 / # pass 152 / # fail 0 / # skipped 4`
   — the prior 150-test baseline (146 pass / 4 pre-existing real-Jellyfin
   skips) plus exactly the 6 new tests added for AC1/AC2 (1 golden-value +
   5 HTTP-route-level), all passing, 0 new skips, 0 regressions. The
   verifier re-ran `test` twice independently with identical results. I
   confirmed the actual test files present in this worktree
   (`src/server/partyKey.test.ts`, `src/server/server.test.ts`) match the
   file contents and test names cited in this count exactly (same 6 new
   test description strings), which corroborates the reported numbers
   rather than leaving them as a bare assertion. Note: the proof doc's
   command-run path is stated as
   `sessions/party-compat-test-coverage.1783309702317` — a different
   worktree/session directory than the one this approval pass runs from
   (`sessions/party-compat-test-coverage.1783310153370`). This is not a
   staleness or leaked-artifacts concern under the proof-path rule (that
   rule targets proof docs whose cited evidence resolves into gitignored
   `./artifacts/...` outside `.artifacts/`): both worktrees share the same
   repo history, and the file-content verification performed directly in
   this session's worktree shows byte-identical test files/assertions to
   what the proof describes, so the executed-count claims are corroborated
   by present, current source rather than resting on a stale or
   unreachable path.

## Remaining

None. No unchecked criteria, no visible child subefforts (this is a
top-level effort per its own spec header: "top-level effort, no parent, no
subefforts").

## Evidence

- `efforts/party-compat-test-coverage/PartyCompatTestCoverage.md` — read in
  full for Goals/Nongoals/AC text before evaluating proof.
- `efforts/party-compat-test-coverage/.artifacts/019f3495-1612-71cf-8d95-2b9d125396e1-proof.md`
  (AC1) — sufficient per above; documents hard-coded literals, independent
  re-derivation, and the mutate/fail gap with reasoning for why it's still
  adequate.
- `efforts/party-compat-test-coverage/.artifacts/019f3495-3105-7457-bb87-42d041ade278-proof.md`
  (AC2) — sufficient; documents full alias-pair coverage and the
  `createApp()` diff review.
- `efforts/party-compat-test-coverage/.artifacts/019f3495-4bd6-7296-a123-1c219ddab732-proof.md`
  (AC3) — sufficient; documents fresh green `check`/`test` with exact
  expected counts, re-run twice.
- `efforts/party-compat-test-coverage/.artifacts/019f3571-ef9d-7b28-84eb-aed745f11112-output.md`
  (builder) — corroborates AC1's mutate-and-fail execution detail and AC2's
  extraction rationale.
- `efforts/party-compat-test-coverage/.artifacts/019f358a-9105-786b-9444-16f50a055e20-output.md`
  (independent verifier) — corroborates all three ACs and explicitly flags
  the AC1 process gap with reasoning.
- `src/server/partyKey.test.ts` (read directly in this worktree) — confirms
  the golden-value test exists exactly as described, with matching literal
  strings.
- `src/server/partyKey.ts` (read directly in this worktree) — confirms
  `PARTY_NAMESPACE` and the UUIDv5 algorithm match the proof's description.
- `src/server/server.test.ts` (read directly in this worktree, full file) —
  confirms all five alias-pair tests exist and match the proof's
  description exactly.
- `src/server/server.ts` (grepped/read directly in this worktree, relevant
  sections) — confirms the `createApp()` seam, unconditional production
  call site, and `isEntryPoint` guard match the proof's description of an
  additive-only extraction.

## Missing evidence

None outstanding — all three criteria had sufficient proof plus
source-level corroboration performed in this pass. The one process gap
(verifier's inability to re-run the mutate-`PARTY_NAMESPACE` check
independently due to read-only sandboxing) is noted above and judged
non-blocking, not a missing-evidence gap, because the mutation was actually
executed and its result documented by the builder, and the verifier
substituted an independently-sufficient corroboration (standalone
algorithm reimplementation matching golden values byte-for-byte) rather
than merely asserting the gap away.
