---
prompt_id: 019f358a-723d-74f0-b9c0-4afd9c223b2a
target_agent: gogglebox-verifier
effort_path: efforts/party-compat-test-coverage/PartyCompatTestCoverage.md
output_path: efforts/party-compat-test-coverage/.artifacts/019f358a-9105-786b-9444-16f50a055e20-output.md
base_tag: handoff/019f358a-723d-74f0-b9c0-4afd9c223b2a
session_name: party-compat-test-coverage.1783309702317
---

First, `cd` into `./sessions/party-compat-test-coverage.1783309702317` and run
all commands from there.

## Effort

Read `efforts/party-compat-test-coverage/PartyCompatTestCoverage.md` in full.
The implementation already landed on `main`
(`efforts/party-compat-test-coverage/.artifacts/019f3571-ef9d-7b28-84eb-aed745f11112-output.md`
has the builder's own account): a golden-value test was added to
`src/server/partyKey.test.ts`, an additive `createApp()` seam plus
`require.main === module` guard was added to `src/server/server.ts`, and a new
`src/server/server.test.ts` covers the `/api/group*`↔`/api/party*` alias
parity.

## Your phase: independent verification + proof authoring

You are read-only on source (do not edit `src/`). Do the following:

1. Run `docker compose run --rm check` and `docker compose run --rm test`
   fresh; report exit codes and the test count line.
2. Read `src/server/partyKey.test.ts`'s new golden-value test and confirm the
   expected value is a hard-coded literal string, not derived by re-calling
   `derivePartyKey` and comparing to itself (i.e. confirm it would actually
   catch a namespace regression, not pass vacuously).
3. Read `src/server/server.test.ts` and confirm it exercises, over a real
   `node:http` listener, all of: `/api/group` vs `/api/party` (create),
   `/api/group/verify-pins` vs `/api/party/verify-pins`, `/api/groups` vs
   `/api/parties` (including per-entry `groupKey`/`partyKey` equality),
   `/api/group/clear` vs `/api/party/clear`, and `GET /api/session`'s
   `activeGroupAlias`/`activePartyAlias` equality.
4. Read the `createApp()` extraction in `src/server/server.ts` and confirm it
   is additive only — the production `app.listen(...)` startup path,
   Jellyfin connectivity requirement, and config loading are unchanged for
   the real entrypoint (only reachable differently for tests via the
   `require.main === module` guard).

Write three proof docs (you may create files via shell redirection even
though you have no dedicated write tool) citing what you found in steps 1-4:

- `efforts/party-compat-test-coverage/.artifacts/019f3495-1612-71cf-8d95-2b9d125396e1-proof.md`
  (AC1 — golden-value regression test)
- `efforts/party-compat-test-coverage/.artifacts/019f3495-3105-7457-bb87-42d041ade278-proof.md`
  (AC2 — HTTP-route-level alias parity test)
- `efforts/party-compat-test-coverage/.artifacts/019f3495-4bd6-7296-a123-1c219ddab732-proof.md`
  (AC3 — fresh green check/test with no regression vs. the 150-test baseline)

Flag anything insufficient rather than fixing it — you're read-only on `src/`.

## Output

Write your pass/fail verdict and findings to
`efforts/party-compat-test-coverage/.artifacts/019f358a-9105-786b-9444-16f50a055e20-output.md`.
