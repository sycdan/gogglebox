---
prompt_id: 019f3571-cea3-71c8-8f91-7603b5bfd4e2
target_agent: gogglebox-builder
effort_path: efforts/party-compat-test-coverage/PartyCompatTestCoverage.md
output_path: efforts/party-compat-test-coverage/.outputs/019f3571-ef9d-7b28-84eb-aed745f11112.md
base_tag: handoff/019f3571-cea3-71c8-8f91-7603b5bfd4e2
session_name: party-compat-test-coverage.1783308087827
---

First, `cd` into `./sessions/party-compat-test-coverage.1783308087827` and do
all edits, commands, and commits from there.

## Effort

Read `efforts/party-compat-test-coverage/PartyCompatTestCoverage.md` in full
before starting — Overview, Goals, Nongoals, and all 3 Acceptance Criteria are
already fully specified.

## Your phase: implementation

Implement all 3 acceptance criteria:

1. Golden-value regression test for `derivePartyKey`/`PARTY_NAMESPACE` in
   `src/server/partyKey.ts` — pin a computed hex output for fixed input(s) to
   a hard-coded expected string (not a self-referential re-derive-and-compare).
   Confirm by manually mutating `PARTY_NAMESPACE` locally and observing this
   new test fail, then revert the mutation.
2. In-process HTTP-route-level test proving each `/api/group*` alias
   (`/api/group`, `/api/group/verify-pins`, `/api/groups`, `/api/group/clear`)
   is wired to the identical handler as its `/api/party*` counterpart with
   agreeing response bodies, plus `GET /api/session`'s
   `activeGroupAlias`/`activePartyAlias` agreement. Follow the Goals/Nongoals
   guidance on approach: prefer plain `node:test` + `node:http`/`fetch`
   against an ephemeral `app.listen(0)`, matching existing
   `src/server/*.test.ts` conventions; only add a minimal additive seam to
   `src/server/server.ts` (e.g. an exported `createApp()`) if needed to make
   routes reachable in-process without a live Jellyfin/`.env` — must not
   change any existing runtime behavior, route, handler, or the production
   `app.listen(...)` startup path.
3. Fresh green `docker compose run --rm check` and `docker compose run --rm
   test`, with the previously-passing 150 tests (146 pass / 4 pre-existing
   real-Jellyfin skips) still passing, plus your new tests.

Respect all Nongoals in the spec: no new general-purpose HTTP test framework
unless the plain `node:test`/`fetch` approach proves materially harder; no
restructuring of `server.ts` startup/config/Jellyfin-connectivity; no
user-visible behavior/route/handler/response-shape changes (if you find the
aliases actually disagree, fix only that specific disagreement and call it
out explicitly in your output — don't expand scope); don't cover routes
beyond the named alias pairs; don't touch `src/client/`.

You may edit `src/server/` only (plus `docker compose run --rm check`/`test`
to self-verify). Do not touch other effort directories under `./efforts`.

## Output

Write your summary (what changed, which of AC1-3 you believe are satisfied
and why, the approach chosen for AC2 and why, confirmation of the
mutate-and-fail check for AC1, anything incomplete) to
`efforts/party-compat-test-coverage/.outputs/019f3571-ef9d-7b28-84eb-aed745f11112.md`.
Do not edit the effort spec's checkboxes yourself — that is the approver's job.
