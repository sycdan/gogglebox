---
proof_id: 019f3495-4bd6-7296-a123-1c219ddab732
ac: 3
agent: gogglebox-verifier
effort_path: efforts/party-compat-test-coverage/PartyCompatTestCoverage.md
---

# AC3 — fresh green check/test, no regression vs. 150-test baseline

## Commands run

From `sessions/party-compat-test-coverage.1783309702317` (this effort's own
worktree), fresh (no prior container reuse assumed):

```
docker compose run --rm check
docker compose run --rm test
```

## Results

- `docker compose run --rm check` — exit code 0. `tsc -p tsconfig.json
  --noEmit && tsc -p tsconfig.server.json --noEmit` produced zero errors/
  output beyond npm's unrelated "new version available" notice.
- `docker compose run --rm test` — exit code 0. Final summary line:

```
1..156
# tests 156
# suites 0
# pass 152
# fail 0
# cancelled 0
# skipped 4
# todo 0
```

156 total tests (152 pass, 4 skipped, 0 fail) = the prior 150-test baseline
(146 pass / 4 pre-existing real-Jellyfin skips) plus exactly 6 new tests, all
passing:

- `ok 143 - derivePartyKey golden values match pre-computed hex strings`
- `ok 144 - /api/group and /api/party (create) invoke the identical handler
  with agreeing response bodies`
- `ok 145 - /api/group/verify-pins and /api/party/verify-pins invoke the
  identical handler with agreeing response bodies`
- `ok 146 - /api/groups and /api/parties invoke the identical handler,
  including per-entry groupKey/partyKey equality`
- `ok 147 - /api/group/clear and /api/party/clear invoke the identical
  handler with agreeing response bodies`
- `ok 148 - GET /api/session returns activePartyAlias and activeGroupAlias
  with equal values`

The 4 skips are the same pre-existing real-Jellyfin skips (no new skips
introduced). No regression in the previously-passing count; only additions.

Command was re-run twice during this verification pass with identical
results both times (156/152/0/4).

## Verdict

Pass. Fresh `check` and `test` runs from this effort's worktree are both
green with the expected test count (150-test baseline + 6 new tests, 0
failures, 0 new skips).
