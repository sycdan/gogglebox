# Proof: group selection + Jellyfin player handoff for token-authenticated accounts

## Criterion

Existing group selection and Jellyfin player handoff continue to work for
token-authenticated accounts in both sandbox proof and real-data UAT where
available.

## How proved (sandbox)

Sandbox stack, two flows under `PROOF_RUN_ID=authproof-20260704T202336`:

- `PROOF_FLOW=group-alias` (`e2e/flows/group-alias.mjs`) — household account
  (token-authenticated via sbx `ACCESS_TOKEN` auto-login, itself an access
  token) creates a group (Alice + Bob), the alias "Alice + Bob" is saved and
  reused without duplicating the group.
- `PROOF_FLOW=player-handoff` (`e2e/flows/player-handoff.mjs`), run against
  the same-origin proxy (`-e PROOF_URL=http://proxy:8080`) as required for
  origin-matched localStorage seeding.

## Evidence

- `artifacts/authproof-20260704T202336/2026-07-05T00-26-31-139Z-group-alias/group-alias-created.png`
  — the household group (Alice + Bob) is active; "Continue watching" and
  "Because none of you have seen this" rails render normally with the group's
  member avatars on cards, and the header shows the "Alice + Bob" active-group
  alias, "Change viewers", and "Log out" controls — confirming group
  selection continues to work end-to-end for a token-authenticated account.
- `artifacts/authproof-20260704T202336/2026-07-05T00-26-56-037Z-player-handoff/player-handoff-gbx.png`
  — the gbx app's own player-launch panel ("NOW PLAYING: Penultimate") opens
  before handing off to the `/player` tab.
- `artifacts/authproof-20260704T202336/2026-07-05T00-26-56-037Z-player-handoff/player-handoff-jellyfin-loggedin.png`
  — the `/player` tab (proxied Jellyfin-web) shows the fully logged-in
  Jellyfin home screen (Home / Movies / Shows / Next Up rails) with no manual
  login form, confirming the seeded localStorage credentials auto-logged the
  correct per-group Jellyfin user into `/player`.
- `artifacts/authproof-20260704T202336/2026-07-05T00-26-56-037Z-player-handoff/player-handoff-jellyfin-playing.png`
  — flow log confirms actual video playback progressed in the `/player`
  iframe: `videoPresent: true, videoPaused: false, playbackStarted: true,
  maxCurrentTime: 0.395653, stream errors = []`, and the played item id
  matches the item gbx handed off (`65ad617478d5be42d553ad0685ff20ef`).

Both flows exited 0 (`[proof] OK`).

## UAT (real-data) coverage

Not run in this pass. This environment has a provisioned sandbox stack only;
no `.env.uat` / real Jellyfin connection was configured or available in the
session worktree, so `./scripts/uat.sh` was not exercised. Per the effort's
own wording ("...in both sandbox proof and real-data UAT **where
available**"), UAT coverage is explicitly conditional — it is not available
in this environment and is called out here rather than silently skipped.

## Verdict

Proved for the sandbox half of this criterion (group selection + player
handoff both work for token-authenticated accounts). The UAT/real-data half
is NOT covered in this pass because no real Jellyfin was available in this
worktree/session — flagged explicitly, not silently skipped.
