# Proof: mixed-group (non-primary) watch-progress warning

## Criterion

Continuing with a group that contains non-primary users shows a confirmation
warning that watch progress will be affected for every user in the group.

## How proved (partial)

Sandbox stack, `PROOF_FLOW=group-pin` (`e2e/flows/group-pin.mjs`), visitor
account, under `PROOF_RUN_ID=authproof-redo-20260705` (fresh sandbox
re-provisioned in this session's worktree). Dave (secondary) + Carol
(tertiary guest, added via "+ Add guest") is a group with no primary members —
a mixed/non-primary group.

## Evidence (copied into this effort's `.proofs/`, not `./artifacts`)

- Source: `src/client/App.tsx` renders a `.confirm-modal` with heading
  "Shared watch progress" and body text "...will affect watch progress and
  watched states for ALL users in the group." whenever the forming group
  contains any non-primary member (comment at `App.tsx:880`: "warn when the
  group contains ANY non-primary member (watch progress is...)").
- Flow assertion: after a successful `POST /api/group/verify-pins`, the flow
  explicitly waits for `.confirm-modal` to become visible
  (`confirmModal.waitFor({ state: 'visible' })`) and fails the run loudly
  (`group-pin: the mixed-group confirmation modal did not appear before the
  group POST`) if it does not — this executed and passed in this run (no
  failure raised), then the flow clicks the modal's "Confirm" button before
  the group POST fires and the picker is left.
- The flow also proves the NEGATIVE case is respected: when the wrong PIN was
  submitted, the flow explicitly asserts the mixed-group modal did **not**
  appear (`group-pin: the mixed-group warning appeared for a wrong PIN —
  verification must happen first` would have failed the run), confirming the
  warning is gated correctly on successful PIN verification rather than
  appearing unconditionally. This run reproduced that same pass (see
  `efforts/auth-refactor/.proofs/019f2fc4-08aa-783a-b276-4a74853259a1.png`,
  the wrong-PIN screenshot, which shows the picker/PIN modal — NOT the
  mixed-group modal — still open).

## Gap — still no screenshot of the warning modal itself (unchanged)

`e2e/flows/group-pin.mjs` was re-inspected in this pass and is byte-for-byte
the same on this point as the prior session: `confirmPinsAndMixed()` still
waits for `.confirm-modal` to become visible and immediately clicks "Confirm"
with **no** intervening `shoot()` call, so no PNG of the open "Shared watch
progress" modal was produced by this run either (confirmed: no
`*mixed*` file exists under this run's `./artifacts` batch). The warning's
presence is proved by a passing Playwright element-visibility assertion tied
to real server state (`verify-pins` 200 gating), not by a screenshot a human
can read directly. Per this prover's constraints (read-only on
`src/`/`e2e/`), closing this requires `gogglebox-builder` to add one
`shoot(page, 'group-pin-mixed-warning')` call in
`e2e/flows/group-pin.mjs`, right after `confirmModal.waitFor(...)` and before
the `Confirm` click, so a future prover pass can capture and copy the warning
modal screenshot into this effort's `.proofs/`.

## Verdict

Partially proved (unchanged from the prior pass on substance, now re-run with
durable evidence for the surrounding steps). The warning's appearance is
confirmed via a real, passing DOM-visibility assertion (and its absence on the
wrong-PIN path is also confirmed), but not via a screenshot showing the actual
warning text on screen. This criterion cannot be fully closed by a prover
alone — it needs the one-line flow change above from `gogglebox-builder`.
