# Approval — auth-refactor (prompt 019f2fb0-92d9-7ae0-b673-1a30355619e4)

- status: fail
- effort: `efforts/auth-refactor/AuthRefactor.md`

## Checked

None. No acceptance criterion is checked in this pass.

## Remaining (all six criteria, unchecked)

1. **Config accounts/access_tokens/migration** — proof link
   `./.artifacts/019f2aa8-492f-70f0-984e-d190346c435b-proof.md` does not exist. Per
   AGENTS.md, a missing proof file means the criterion is not yet checkable,
   regardless of how strong the underlying implementation looks. I read
   `src/server/config.ts` and `src/server/config.test.ts` directly (in the
   session worktree) and found real, substantive coverage — `accounts`/
   `access_tokens` records, a migration chain asserted to go schemaVersion
   0→1→2 in tests, malformed/orphaned-token dropping with warnings, and
   fail-fast only when zero accounts or zero tokens survive — and the
   verifier's static-verification output
   (`.artifacts/019f2f9e-2acd-735e-bbce-7af071a5a490-output.md`) shows the full suite
   (145 tests, 141 pass, 0 fail, 4 skipped) passing. This is good evidence,
   but it was never captured into a dedicated proof file for this criterion,
   so I did not check it off. Needs: a proof doc at the linked UUID citing
   the specific config.test.ts cases (migration chain, token-uniqueness/
   dedup behavior) as this criterion's evidence.

2. **Portal login accept/reject/persist** — proof exists
   (`./.artifacts/019f2aa8-4931-7a1c-86ba-4785a38163c3-proof.md`) and is explicit that
   the invalid-token rejection branch was never exercised by any flow — no
   screenshot or flow assertion of a deliberately wrong token producing a
   "clear error." The criterion requires reject-with-clear-error as one of
   its three conjuncts; that conjunct is unproven. Needs: a new/extended
   `e2e/flows/*.mjs` step submitting a bad access token and asserting/
   screenshotting the resulting `.error` text with the login form still
   present.

3. **Account user rendering (primary preselected/secondary visible/add-guest
   sizing)** — proof doc claims full confirmation via
   `group-alias-picker.png` and `group-pin-picker.png`, and the corresponding
   flow source (`e2e/flows/group-pin.mjs`, read in full) does contain real,
   non-stubbed assertions (exact viewer-card name-set checks, an
   `add-guest-card` existence check) that are consistent with the criterion.
   However, the prompt explicitly requires reading the referenced
   screenshots rather than trusting the prover's self-report, and the
   `artifacts/authproof-20260704T202336/...` directory these screenshots
   live in does not exist anywhere in the repo or on disk — it was written
   under a different, now-removed session worktree
   (`./sessions/auth-refactor.1783210783732`, not this one), and `artifacts/`
   is gitignored so it was never committed. I could not view a single one of
   the cited PNGs. Needs: either the screenshots re-generated and reachable
   from the session actually being approved, or copied into
   `.artifacts/` per AGENTS.md's "proof... may copy evidence from root
   `./artifacts` into the effort's `.artifacts/` directory" so they survive
   worktree teardown.

4. **Add-guest flow (tertiary listing/PIN requirement/confirm gating)** —
   same screenshot-unavailability problem as (3). The flow source logic
   (disabled-until-typed assertion, 403-on-wrong-PIN gated on a real
   `POST /api/group/verify-pins` response, success-on-correct-PIN) is real
   and consistent with the criterion, but the cited
   `group-pin-prompt.png` / `group-pin-wrong.png` / `group-pin-success.png`
   files are unreachable for the same reason as above. Needs: same as (3).

5. **Mixed-group watch-progress warning** — proof doc itself already flags
   this as only partially proved: no screenshot of the "Shared watch
   progress" modal exists even in the original session (the flow clicks
   through it without an intervening `shoot()` on the happy path), so this
   was never going to be fully proved regardless of artifact availability.
   The positive/negative DOM-visibility assertions are real (confirmed by
   reading `e2e/flows/group-pin.mjs` directly — `confirmModal.waitFor` on
   the happy path, an explicit check that it does NOT appear on the
   wrong-PIN path), but a DOM-assertion pass is not the same as visually
   confirming the warning text renders correctly for a human. Needs: add
   `shoot(page, 'group-pin-mixed-warning')` right after
   `confirmModal.waitFor(...)` and before the `Confirm` click, then re-prove.

6. **Group selection + player handoff for token-authenticated accounts** —
   same screenshot-unavailability problem as (3)/(4) for the sandbox half
   (`group-alias-created.png`, `player-handoff-*.png`); additionally the
   proof doc explicitly states UAT/real-Jellyfin coverage was never run
   (no `.env.uat` available in that session). Even setting the artifact
   problem aside, this criterion's UAT half remains unproven. Needs: (a)
   reachable screenshots for the sandbox half, (b) a UAT pass through
   `./scripts/uat.sh` with a real Jellyfin once available.

## Evidence read

- `efforts/auth-refactor/AuthRefactor.md` — full spec, six criteria.
- `efforts/auth-refactor/.artifacts/019f2f9e-2acd-735e-bbce-7af071a5a490-output.md` —
  verifier pass (typecheck + 145 unit tests, 0 fail). Sufficient corroboration
  for criterion 1's *implementation* but not a dedicated proof link.
- `efforts/auth-refactor/.artifacts/019f2fa5-339b-7602-a96c-1f9e65a9a773-output.md` —
  prior prover's partial self-report; read in full, cross-checked against its
  own linked proof docs rather than trusted at face value.
- `efforts/auth-refactor/.artifacts/019f2aa8-4931-7a1c-86ba-4785a38163c3-proof.md`,
  `.../019f2aa8-4933-796a-977a-7b9c756a7040.md`,
  `.../019f2aa8-4934-7057-ac9a-c1ce09978cf1.md`,
  `.../019f2aa8-4936-7965-b407-de265936e392.md`,
  `.../019f2aa8-4937-78c2-84fc-7c0eadca4372.md` — all read in full. Each is
  internally honest about its own gaps (criteria 2, 5, 6 all self-report
  partial), and criteria 3/4/6's "confirmed" claims rest on screenshots I
  could not independently view this pass.
- `src/server/config.ts`, `src/server/config.test.ts` — read directly
  (session worktree) to independently corroborate criterion 1's
  implementation; found real accounts/access_tokens handling and a tested
  0→1→2 migration chain, but this is not a substitute for a proof file at
  the criterion's own linked UUID.
- `e2e/flows/group-pin.mjs` — read in full to independently corroborate the
  *logic* behind criteria 3, 4, and 5's DOM assertions (all real, gated on
  actual server responses, not stubbed).
- Confirmed via `Glob`/`Grep` across the repo and `.git` that
  `artifacts/authproof-20260704T202336/**` does not exist anywhere on disk
  and was never committed (expected — `artifacts/` is gitignored), and that
  the session worktree that generated it
  (`./sessions/auth-refactor.1783210783732`) no longer exists (only
  `sessions/.gitkeep` remains).

## Missing evidence (blocking every criterion)

- Criterion 1: a proof file must be created at
  `./.artifacts/019f2aa8-492f-70f0-984e-d190346c435b-proof.md` citing the config
  schema/migration/token-uniqueness test evidence.
- Criteria 2, 5: functional gaps in the flows themselves (invalid-token
  rejection step; a screenshot call for the mixed-group warning) — these
  need new coverage, not just re-running existing flows.
- Criteria 3, 4, 6 (sandbox half): the cited screenshots need to exist and
  be reachable at approval time — either by approving in the same session
  that generated them (before its worktree is torn down) or by copying the
  PNGs into `.artifacts/` as AGENTS.md permits, so approval doesn't depend on a
  since-deleted worktree.
- Criterion 6 (UAT half): an actual `./scripts/uat.sh` run against a real
  Jellyfin has not happened yet in any session.

## Overall

The effort is **not done**. No acceptance criteria are checked in this pass.
The effort spec was not edited (no `[ ]` → `[x]` changes) because none of the
six criteria met the bar for sufficient, currently-verifiable proof.
