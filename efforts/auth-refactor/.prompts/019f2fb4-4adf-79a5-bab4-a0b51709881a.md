---
prompt_id: 019f2fb4-4adf-79a5-bab4-a0b51709881a
target_agent: gogglebox-prover
effort_path: efforts/auth-refactor/AuthRefactor.md
output_path: efforts/auth-refactor/.outputs/019f2fb4-4adf-79a5-bab4-a0b51709881a.md
base_tag: handoff/019f2fb4-4adf-79a5-bab4-a0b51709881a
session_name: auth-refactor.1783211772710
---

First instruction: `cd` into `./sessions/auth-refactor.1783211772710` before
anything else. Run all commands from there.

## Context

This is a redo of an earlier prover pass for `efforts/auth-refactor/AuthRefactor.md`
(top-level effort). The prior pass wrote proof docs at
`efforts/auth-refactor/.proofs/*.md` that only *referenced* PNGs under
`./artifacts/...`, which is gitignored — once that session's worktree was torn
down, the screenshots were gone and the approver could not verify anything.
Read the existing proof docs first (`git log --follow` / just open them) to see
what's already claimed, then fix both the coverage gaps and the durability
problem below. Static verification (typecheck/tests) already passed
(`efforts/auth-refactor/.outputs/019f2f9e-2acd-735e-bbce-7af071a5a490.md`).

## Hard requirement: proofs must be self-contained

For every criterion you confirm, **copy the relevant screenshot PNG(s) out of
`./artifacts/...` into `efforts/auth-refactor/.proofs/` itself** (e.g.
`efforts/auth-refactor/.proofs/019f2aa8-....png`) and reference that copied,
in-tree path from the proof markdown — not the ephemeral `./artifacts` path.
`.proofs/` is not gitignored, so this is what makes the evidence survive the
worktree/session being torn down.

## Gaps to close (rewrite the affected proof files)

1. **Criterion 1** (config schema/migration) — no proof file exists yet at
   `./.proofs/019f2aa8-492f-70f0-984e-d190346c435b.md`. This one isn't a UI
   criterion; write the proof file citing the specific test names/assertions in
   `src/server/config.test.ts` and `src/server/configMigrations.test.ts` that
   cover `accounts[account_key]`, `access_tokens[token] -> account_key`, token
   uniqueness validation, and the migration chain, plus a reference to the
   passing verifier run. No screenshot needed for this one.
2. **Criterion 2** (login accept/reject/persist) — existing proof
   (`./.proofs/019f2aa8-4931-7a1c-86ba-4785a38163c3.md`) never exercised
   invalid-token rejection. Add a flow step (or new flow) that submits a
   deliberately invalid access token, screenshot the clear error state, copy
   that PNG into `.proofs/`, and update the proof doc.
3. **Criterion 5** (mixed-group warning) — existing proof
   (`./.proofs/019f2aa8-4936-7965-b407-de265936e392.md`) only has a DOM
   assertion, no screenshot of the warning modal. Add a
   `shoot(page, 'group-pin-mixed-warning')` (or equivalent) call right before
   the mixed-warning modal's confirm action in the relevant flow, capture it,
   copy the PNG into `.proofs/`, and update the proof doc.
4. **Criterion 6** (sandbox + UAT) — sandbox half was already proven; a real
   Jellyfin UAT config exists at `.env.uat` / `config.uat.json` in the repo
   root, unlike the prior session which reported none available. Run the
   relevant flow(s) via `./scripts/uat.sh` against it, screenshot the result,
   copy the PNG into `.proofs/`, and update the proof doc to cover both halves.
   If the UAT Jellyfin is unreachable from this environment, say so explicitly
   with the actual error, rather than skipping silently.
3. **Criteria 3 & 4** were already fully confirmed in the prior pass with
   screenshots — just copy their existing evidence PNGs into `.proofs/` and
   update those two proof docs' references; no need to re-run those flows.

## Output

Write your final summary — what you proved, which files/PNGs you added under
`.proofs/`, and any criterion still not fully closeable and why — to
`efforts/auth-refactor/.outputs/019f2fb4-4adf-79a5-bab4-a0b51709881a.md`. You
are read-only on `src/`; you may only write under `efforts/auth-refactor/`.
