# Prover output — auth-refactor redo (prompt 019f2fb4-4adf-79a5-bab4-a0b51709881a)

## Summary

Redid the prior visual-proof pass with durable evidence. Every screenshot this
pass relies on is copied into `efforts/auth-refactor/.proofs/*.png` (not
gitignored) and referenced from the proof markdown by that in-tree path — no
proof doc references `./artifacts` anymore. Fresh sandbox Jellyfin was
provisioned inside this session's own worktree (its Docker volumes/config were
not carried over from the prior torn-down worktree), and a real-Jellyfin UAT
run was also completed against `.env.uat` / `config.uat.json`
(`http://htpc.lan:8096`).

## Criteria status

1. **Config schema/migration** — new proof file written:
   `efforts/auth-refactor/.proofs/019f2aa8-492f-70f0-984e-d190346c435b.md`.
   Cites specific named tests in `src/server/config.test.ts` and
   `src/server/configMigrations.test.ts` covering `accounts[account_key]`,
   `access_tokens[token] -> account_key`, transitive token-uniqueness
   de-duplication, and the full 0->1->2 migration chain, plus the passing
   verifier run (`efforts/auth-refactor/.outputs/019f2f9e-2acd-735e-bbce-7af071a5a490.md`).
   No screenshot needed. **Proved.**

2. **Login accept/reject/persist** — updated
   `019f2aa8-4931-7a1c-86ba-4785a38163c3.md` with a freshly copied
   `group-pin-login.png` (accept path). Re-inspected
   `e2e/flows/group-pin.mjs` and all other flow modules: none of them submit a
   deliberately invalid access token, so the reject-with-clear-error half is
   **still not visually proved**. This needs a `gogglebox-builder` change (a
   flow step that submits a bogus token, asserts a 4xx + `.error` text +
   login form remaining, and screenshots it) — I did not add it myself since
   I'm read-only on `src/`/`e2e/`. **Partially proved.**

3. **Account user rendering** — updated
   `019f2aa8-4933-796a-977a-7b9c756a7040.md` with two freshly copied,
   re-verified screenshots (`group-alias-picker.png`,
   `group-pin-picker.png`) showing primary preselection, secondary
   unselected-visible, and add-guest sizing parity. **Proved.**

4. **Add-guest flow (tertiary listing + PIN gating)** — updated
   `019f2aa8-4934-7057-ac9a-c1ce09978cf1.md` with three freshly copied
   screenshots (PIN-modal disabled state, wrong-PIN rejection, correct-PIN
   success). **Proved.**

5. **Mixed-group warning modal** — updated
   `019f2aa8-4936-7965-b407-de265936e392.md`. Re-inspected
   `e2e/flows/group-pin.mjs`: `confirmPinsAndMixed()` is unchanged from the
   prior pass — it still waits for `.confirm-modal` visibility and clicks
   "Confirm" immediately with no `shoot()` call in between. Confirmed no
   `*mixed*` screenshot file exists in this run's artifacts either. The
   warning's appearance/absence is still only proved via a passing DOM
   assertion, not a screenshot a human can read. Needs one
   `shoot(page, 'group-pin-mixed-warning')` line from `gogglebox-builder`
   before/instead of the immediate confirm click. **Partially proved
   (unchanged from prior pass on substance; now durable).**

6. **Group selection + player handoff (sandbox + UAT)** — updated
   `019f2aa8-4937-78c2-84fc-7c0eadca4372.md`.
   - **Sandbox**: full pass, including actual playback progress inside the
     `/player` iframe (`player-handoff` flow), all with freshly copied
     screenshots.
   - **UAT (real Jellyfin)**: ran against `.env.uat`/`config.uat.json`
     (`JELLYFIN_URL=http://htpc.lan:8096`, reachable — confirmed
     `GET /System/Info/Public` -> HTTP 200 — server logs show it built config
     from the real instance). `group-alias` flow failed because it hard-codes
     the sandbox's two-primary fixture and the real config only has one
     primary (Alice); `player-focus` flow got past login + group selection
     into the fully-rendered real-data main app, then failed on a Playwright
     strict-mode selector ambiguity (two Play/Resume buttons matched) when
     trying to click into the player — not an app defect. Screenshots from
     both attempts are copied in as partial UAT evidence (real picker with
     Alice/real avatar; real main app with real posters/titles/Resume state).
     Full player-handoff click-through against real data was **not**
     completed — this needs a UAT-safe flow variant from
     `gogglebox-builder`. **Partially proved** (sandbox proved fully;
     UAT proved through group selection + real main-app rendering, not
     through to player click-through).

## Files changed under `efforts/auth-refactor/`

- New: `.proofs/019f2aa8-492f-70f0-984e-d190346c435b.md` (criterion 1)
- Updated: `.proofs/019f2aa8-4931-7a1c-86ba-4785a38163c3.md` (criterion 2)
- Updated: `.proofs/019f2aa8-4933-796a-977a-7b9c756a7040.md` (criterion 3)
- Updated: `.proofs/019f2aa8-4934-7057-ac9a-c1ce09978cf1.md` (criterion 4)
- Updated: `.proofs/019f2aa8-4936-7965-b407-de265936e392.md` (criterion 5)
- Updated: `.proofs/019f2aa8-4937-78c2-84fc-7c0eadca4372.md` (criterion 6)
- New PNGs (12 total) under `.proofs/`, each referenced by its exact path from
  the proof doc that relies on it — see each doc for the mapping.

## Environment notes (for whoever runs this next)

- This worktree does not carry gitignored `.env`, `.env.sbx`, `.env.uat`,
  `config.sbx.json`, `config.uat.json` (git worktrees only contain tracked
  files). I copied these in from the repo root to run the stacks; they are
  gitignored so they do not show up in `git status` here and were not
  committed.
- Sandbox Docker volumes are per-worktree-directory-name (compose project
  name derives from the folder), so this worktree's sandbox Jellyfin started
  empty — I ran `sandbox-generate` + `sandbox-provision` fresh before proof
  flows would work.
- An unrelated, already-running `gogglebox-proxy-1` (main checkout, real
  Jellyfin `htpc.lan`) held host port 8080 for the whole session. For the one
  flow needing this worktree's own proxy (`player-handoff`, sandbox), I used a
  temporary, non-committed `docker-compose.local.yml` override
  (`services: proxy: ports: !override []`) to stop this worktree's proxy from
  trying to publish 8080 to the host — container-to-container
  `http://proxy:8080` still worked fine without a host port. That temp file
  was deleted before finishing; it never touched `src/` or any tracked file.
- Both stacks (`sbx`, `uat`) I brought up in this worktree were torn down
  (`./scripts/uat.sh down --remove-orphans`) before finishing; the unrelated
  main-checkout `gogglebox-*` stack was left untouched throughout.

## Next action

Send criteria 2, 5, and the UAT half of criterion 6 back to
`gogglebox-builder` for the three flow additions described above (invalid
access-token rejection step, one `shoot()` call before the mixed-warning
confirm click, and a UAT-safe player-handoff-style flow that doesn't assume
sandbox-only fixture shapes or hit the Play/Resume selector ambiguity against
a real, larger library). Criteria 1, 3, and 4 are ready for
`gogglebox-approver`.
