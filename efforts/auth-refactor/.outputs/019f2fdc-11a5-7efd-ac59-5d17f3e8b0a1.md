# Output: gogglebox-prover — close criteria 2, 5, 6(UAT) for AuthRefactor

## What I did

Worked entirely from `./sessions/auth-refactor.1783214379729` as instructed.
Provisioned a fresh sandbox (`sandbox-generate` + `sandbox-provision`, since
this worktree had no `.env.sbx`/`config.sbx.json` yet), brought up
server+client, and ran the flows the builder added:

1. `PROOF_FLOW=group-pin` (sandbox) — now includes the invalid-access-token
   rejection step and a `shoot()` before the mixed-group warning modal's
   confirm click. Exited 0. Produced
   `group-pin-invalid-token.png` and `group-pin-mixed-warning.png`.
2. `PROOF_FLOW=player-uat` (sandbox) — new UAT-safe generic click-through flow.
   Exited 0 against sandbox (2 preselected primaries, real modal open, `/player`
   settled logged-in).
3. `PROOF_FLOW=player-uat` against the **real UAT Jellyfin**
   (`.env.uat`/`config.uat.json`, `htpc.lan:8096`, copied from the main
   worktree since they're gitignored generated/hand-created files not present
   in a fresh worktree). `htpc.lan` was reachable from this environment.
   Exited 0: one preselected primary (Alice, the real single-primary shape),
   opened the player modal for a real title ("(500) Days of Summer"), and the
   `/player` Jellyfin child frame settled into a genuine logged-in state
   (`loggedInVisible: true, loginFormVisible: false`) with a real resume
   position and serverId in the URL hash — no manual login form.

For the host port-8080 conflict (another unrelated stack already had it
bound), I used the same workaround as the prior pass: a local, non-committed
`docker-compose.local.yml` that resets this project's `proxy` service's `ports:`
to empty, layered on top of the run stack via an explicit
`docker compose -f ... -f ... -f docker-compose.local.yml` invocation. This
does not change the app's own configuration, only how the container's port is
published to the host for this session.

I Read every screenshot below directly (not just checked flow logs) before
writing the proof docs.

## Proof docs updated

- `efforts/auth-refactor/.proofs/019f2aa8-4931-7a1c-86ba-4785a38163c3.md`
  (criterion 2) — now marked **fully proved**. Added the invalid-token
  rejection evidence: `group-pin-invalid-token.png` shows the login form with
  the bogus token still filled, the "Invalid access token" error text visible,
  and the form still on screen (not authenticated). Combined with the
  pre-existing valid-token-accept and logout-clears evidence, all three
  criterion clauses are now visually confirmed.
- `efforts/auth-refactor/.proofs/019f2aa8-4936-7965-b407-de265936e392.md`
  (criterion 5) — now marked **fully proved**. Added
  `group-pin-mixed-warning.png`, a direct screenshot of the open
  "Shared watch progress" confirm-modal with its full warning text ("...will
  affect watch progress and watched states for ALL users in the group."),
  closing the previous gap where the warning was only proved via a passing
  DOM-visibility assertion, not an image a human could read.
- `efforts/auth-refactor/.proofs/019f2aa8-4937-78c2-84fc-7c0eadca4372.md`
  (criterion 6) — now marked **fully proved for both halves**. Sandbox half
  was already proved in the prior pass (unchanged). UAT half is now closed:
  the new `player-uat` flow completed a full click-through against the real
  Jellyfin at `htpc.lan`, with two screenshots showing the player modal open
  over the real library and the `/player` iframe settled logged-in with real
  data (resume position + serverId), and no manual login form.

## Evidence copied into `efforts/auth-refactor/.proofs/` (durable, not gitignored)

- `019f2fe2-9f63-7f0d-b2d2-dc66ecc0eb95.png` — invalid access token rejected,
  login form persists, error visible (criterion 2).
- `019f2fe2-9f64-7212-b76f-1289997defc7.png` — mixed-group "Shared watch
  progress" warning modal, open, full text visible (criterion 5).
- `019f2fe2-9f64-74bc-904d-c9fe4b3c6140.png` — `player-uat` sandbox run,
  player modal + settled `/player` frame (criterion 6, sandbox half,
  supplementary).
- `019f2fe2-9f64-7ad6-b8e7-872b31dfa8bf.png` — `player-uat` **UAT** run,
  player modal open over the real library (criterion 6, UAT half).
- `019f2fe2-9f64-7781-9a53-54758a5cdca5.png` — `player-uat` **UAT** run,
  `/player` frame settled logged-in with real resume data (criterion 6, UAT
  half).

## Are all six criteria now fully provable?

Yes, as far as this prover can determine:

- Criteria 1, 3, 4 — left as-is per the handoff (already fully proved with
  durable evidence from the prior pass); I did not re-inspect their docs
  beyond confirming they still reference existing copied PNGs.
- Criteria 2, 5 — now fully proved (this pass).
- Criterion 6 — now fully proved for both sandbox and UAT halves (this pass
  closes the previously-open UAT half).

All flows run in this pass exited 0 (`[proof] OK`); nothing was force-passed
or inferred without a screenshot I actually Read.

## Limitations / notes for the orchestrator

- I copied `.env.uat`/`config.uat.json` from the main worktree into this
  session's worktree to reach the real Jellyfin (they're gitignored
  hand-created files, absent from a fresh worktree by design). They remain
  untracked/gitignored here too — nothing sensitive was staged or committed.
- `docker-compose.local.yml` (untracked, not gitignored, not staged) is a
  scratch host-port workaround for this session only; the orchestrator can
  ignore/delete it along with the rest of this worktree during teardown.
- I did not touch `e2e/`, `src/`, or any other application code — proof-only,
  as required.
