# Prompt: release-e2e-gate — approve AC4 + AC7 (durable evidence now in .proofs/)

prompt_id: 019f3342-c252-7258-8f5a-3eb40a9fe191
target: gogglebox-approver
output_path: ./.outputs/019f3342-d953-7107-97a7-da1b3fefdf1d.md

## Context

You (a prior approver pass) correctly left AC4 and AC7 unchecked because the
live-CI evidence for run `28748139115` existed only as chat narrative, not as
a durable file in the repo. That gap is now fixed: the orchestrator copied
the actual downloaded artifacts into
`efforts/release-e2e-gate/.proofs/` directly (not `./artifacts`, so no
gitignore/worktree-teardown risk):

- `live-run-28748139115-group-alias-02-authenticated.png` — the real
  107826-byte screenshot extracted from the downloaded `e2e-artifacts` CI
  artifact zip (the artifact GitHub generated from the "Upload artifacts on
  failure" step of the real run).
- `live-run-28748139115-jobs.json` — raw
  `gh api repos/sycdan/gogglebox/actions/runs/28748139115/jobs` output.
  Confirms job `e2e` (id 85242489599) has `"conclusion":"failure"`, its step
  "Run e2e flow suite" has `"conclusion":"failure"`, its step "Upload
  artifacts on failure" has `"conclusion":"success"`, and job `publish` (id
  85242605496) has `"conclusion":"skipped"` with an empty `"steps":[]`
  (proving it never ran any step, i.e. was skipped by `needs:` propagation,
  not merely fast/no-op).
- `live-run-28748139115-artifacts.json` — raw
  `gh api .../actions/runs/28748139115/artifacts` output, showing artifact
  `e2e-artifacts`, `size_in_bytes: 101615`, `expired: false`, a real
  `archive_download_url`.
- `live-run-28748139115-log-failed.txt` — raw
  `gh run view 28748139115 --log-failed` output, containing the literal line
  `[proof] FAIL: DELIBERATE scratch/e2e-gate-live-proof failure — proving
  AC4/AC7 artifact-upload + publish-skip against a real Actions run. Revert
  before merge.` followed by `##[error]Process completed with exit code 1.`

Background on how this run was produced (for context only — the durable
files above are what should drive your decision, not this narrative): a
scratch branch `scratch/e2e-gate-live-proof` was cut from `main`, one flow
(`e2e/flows/group-alias.mjs`) was made to fail unconditionally, pushed to
`origin`, and `publish.yml` was dispatched via `workflow_dispatch` against
that branch. The branch has since been deleted both locally and on `origin`;
`main` was never touched by the deliberate breakage.

## What to do

1. Read the 4 files above under `.proofs/` directly.
2. Re-evaluate AC4 and AC7 in `ReleaseE2eGate.md` against this durable
   evidence.
3. If sufficient, update `.proofs/019943aa-1a1e-7f83-b4e1-9a6d2c8f5b34.md`
   (AC4) and `.proofs/019943aa-1a21-7ef6-a7b4-8d9c5f1e2a67.md` (AC7) to cite
   these new evidence files by name, and check the corresponding boxes in
   `ReleaseE2eGate.md`.
4. Write your final summary to the declared `output_path` above.

## Scope

Your edits are limited to `efforts/release-e2e-gate/` per your standing
restriction to `efforts/`.
