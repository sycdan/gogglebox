# Proof — AC7: `publish` needs both `test` and `e2e`; failure-skip (SUFFICIENTLY PROVEN)

## Criterion

`publish`'s `needs:` includes both `test` and `e2e`, and that a
deliberately-broken flow (temporary local/dry-run repro, reverted after
proving) causes `publish` to be skipped exactly as a deliberately-broken
unit test does today, while `release.yml`'s promotion behavior is unchanged
(no retest, no rebuild).

## Evidence reviewed

- `.github/workflows/publish.yml` (line 167) reads directly:

  ```yaml
  publish:
    needs: [test, e2e]
  ```

  This satisfies the static `needs:` half of the criterion by direct
  inspection — confirmed today, in the repo, not just asserted by a builder.

- `release.yml` was not touched by this effort (only `publish.yml` appears
  in the builder handoffs' file lists), and its promotion behavior —
  promoting an already-built, already-tested image without
  rebuilding/retesting — is unaffected by construction, since no proof
  or diff in this effort edits it.

- `.proofs/live-run-28748139115-jobs.json` — raw `gh api
  repos/sycdan/gogglebox/actions/runs/28748139115/jobs` output, read directly
  in this pass. Shows the `publish` job (id `85242605496`) on the real run
  with `"conclusion":"skipped"` and `"steps":[]` (an empty steps array,
  `created_at`/`started_at`/`completed_at` all equal to
  `2026-07-05T16:59:34Z`, `runner_id`/`runner_name` both `null`). An empty
  steps list with a null runner proves the job never actually started
  executing any step on any runner — this is `needs:`-propagation skip
  behavior, not a job that ran and merely finished fast or no-op'd.

- The same file confirms `e2e` (id `85242489599`) has `"conclusion":
  "failure"`, which is the direct cause of `publish`'s skip per
  `needs: [test, e2e]` semantics: GitHub Actions skips a job when any of its
  `needs:` dependencies fails, unless the skipped job declares its own
  `if:` override (none present in `publish.yml`).

- `.proofs/live-run-28748139115-log-failed.txt` independently confirms the
  `e2e` job's flow suite genuinely failed (deliberate marker + nonzero exit),
  which is the trigger event whose downstream effect (`publish` skipped) is
  what `jobs.json` records.

## Why this is now sufficient

Both halves of the conjunctive criterion are now backed by durable evidence
living in `.proofs/`, not chat narrative:

1. **Static `needs:` correctness** — confirmed by direct inspection of
   `.github/workflows/publish.yml` today.
2. **Failure-skip behavior on a real run** — confirmed by
   `live-run-28748139115-jobs.json`, which shows `publish` with an empty
   step list and a null runner (proof it was skipped, not run), directly
   downstream of the `e2e` job's real failure recorded in the same file and
   corroborated by `live-run-28748139115-log-failed.txt`.
3. **`release.yml` unchanged** — true by construction (not edited by this
   effort); no evidence contradicts this.

## Verdict

Sufficient. Checked.
