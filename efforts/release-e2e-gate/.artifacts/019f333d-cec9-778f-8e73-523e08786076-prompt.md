# Prompt: release-e2e-gate — approve AC1-7

prompt_id: 019f333d-cec9-778f-8e73-523e08786076
target: gogglebox-approver
output_path: ./.artifacts/019f333d-fc82-7574-ba87-0b05b00ef094-output.md

## Context

All 7 acceptance criteria in `ReleaseE2eGate.md` have implementation +
evidence gathered across two builder handoffs plus one orchestrator-driven
live CI proof pass. No proof files exist yet under `.artifacts/` (only
`.gitkeep`) — please write one proof doc per AC at its already-seeded
proof-link path and check the criteria that are sufficiently proven.

## Evidence available

**AC1 + AC2** — see `.artifacts/019f301a-1c2b-75ac-be64-a8f5e4d4ff49-output.md`
(gogglebox-builder). `e2e/run.mjs` all-flows mode (`PROOF_FLOW=all`)
implemented, exercised locally against sbx for real across 3 runs (clean
pass, a genuine pre-existing flow bug causing non-zero exit, then a clean
pass again after sandbox-reset), with explicit isolation verification for
`group-alias` -> `group-pin` -> `player-handoff` back-to-back. AC2 (fail()
exit-code propagation) verified by reading `e2e/lib/harness.mjs` AND an
empirical scratch-script run showing `process.exit(1)` fires and no
subsequent line executes.

**AC3, AC5, AC6** — see
`.artifacts/019f3086-7177-7c73-8971-f0c4f3341e04-output.md` (gogglebox-builder).
`.github/workflows/publish.yml` `e2e` job added (parallel to `test`, both
`needs: gate`), full sandbox bootstrap sequence run for real locally
(jellyfin-sandbox up -> sandbox-generate -> sandbox-provision ->
sandbox-reset -> full stack up), client healthcheck polled and confirmed
`"healthy"`, teardown (`down -v --remove-orphans`) confirmed scoped to its
own compose project only. `timeout-minutes: 60` present with a comment
documenting it as an initial tunable, not a permanent constant.

**AC4 + AC7** — NOT covered by local validation alone (builder explicitly
flagged this gap and asked for either static-reasoning acceptance or an
authorized live CI proof). The user authorized a live CI proof pass this
session, which I (orchestrator) executed directly:

- Branch `scratch/e2e-gate-live-proof` cut from `main` at `4e83817`.
- Deliberately broke `e2e/flows/group-alias.mjs` by calling
  `fail('DELIBERATE scratch/e2e-gate-live-proof failure...')` as the first
  line of `run()`.
- Pushed the branch, dispatched `publish.yml` via
  `gh workflow run publish.yml --ref scratch/e2e-gate-live-proof`.
- Real run: https://github.com/sycdan/gogglebox/actions/runs/28748139115
  - `gate` passed (3s), `test` passed (15s).
  - `e2e` failed at the "Run e2e flow suite" step after a full real sandbox
    boot (Jellyfin wizard, fixture generation, provisioning, reset, stack
    up, healthcheck all green) — log line:
    `[proof] FAIL: DELIBERATE scratch/e2e-gate-live-proof failure — proving AC4/AC7 artifact-upload + publish-skip against a real Actions run. Revert before merge.`
    followed by `##[error]Process completed with exit code 1.`
  - "Upload artifacts on failure" step ran (✓ green) and produced artifact
    `e2e-artifacts`, confirmed via
    `gh api repos/sycdan/gogglebox/actions/runs/28748139115/artifacts`:
    `size_in_bytes: 101615`, `expired: false`, real `archive_download_url`.
    Downloaded and unzipped it directly — contains
    `2026-07-05T16-59-29-372Z-all/group-alias/group-alias-02-authenticated.png`,
    a real 107826-byte screenshot. **AC4 fully proven**: artifact uploaded
    on failure AND confirmed retrievable from a real Actions run.
  - "Tear down sandbox stack" step ran (✓ green) despite the failure —
    reconfirms AC5 live, not just locally.
  - `publish` job shows `-` (skipped), 0s runtime — **AC7 fully proven**:
    `needs: [test, e2e]` skip-propagation triggered by `e2e` failing, exactly
    as it does today for a broken unit test. No image was built or pushed to
    GHCR.
- Cleanup: scratch branch deleted both locally and on `origin` (verified via
  `git ls-remote --heads origin` — gone). `main` was never touched by the
  deliberate breakage (it lived only on the scratch branch, which was never
  merged). No re-dispatch was performed after the deliberate breakage was in
  place, so no risk of an accidental real GHCR push.

## What to do

1. Confirm the two builder `.outputs/*.md` files and the live-CI evidence
   above are sufficient proof for AC1-7 (all 7 look complete to the
   orchestrator, but you have final say per the effort-approval process).
2. Write one proof doc per AC at its seeded path (`.artifacts/019943aa-*.md`),
   summarizing the specific evidence for that AC (cite the builder output
   file and/or the live run URL/log lines above — do not just restate the AC
   text).
3. Check each satisfied acceptance criterion box in `ReleaseE2eGate.md`.
4. Write your final summary to the declared `output_path` above, listing
   which criteria were checked, which remain unchecked (if any), and why.

## Scope

Your edits are limited to `efforts/release-e2e-gate/` (spec, `.artifacts/`,
`.outputs/`) per your standing restriction to `efforts/`.
