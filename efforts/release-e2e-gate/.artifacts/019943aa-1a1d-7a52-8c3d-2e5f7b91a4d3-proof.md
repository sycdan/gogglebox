# Proof — AC3: new `e2e` job in `publish.yml`

## Criterion

`publish.yml` gains a new `e2e` job (parallel to `test`, both gated on
`gate`) which bootstraps the sandbox on a clean runner (`jellyfin-sandbox`
up, `sandbox-generate`, `sandbox-provision`, `sandbox-reset`), brings up the
full sandbox stack only after `.env.sbx` and `config.sbx.json` exist, waits
for the client healthcheck before running proof, and runs the full flow
suite via the new all-flows mode through `PROOF_URL=http://proxy:8080`.

## Evidence

Source: `efforts/release-e2e-gate/.artifacts/019f3086-7177-7c73-8971-f0c4f3341e04-output.md`
(gogglebox-builder), cross-checked directly against
`.github/workflows/publish.yml` as it exists in the repo today.

Read `.github/workflows/publish.yml` lines 88-146 directly: the `e2e` job is
present, `needs: gate`, same `if: needs.gate.outputs.bump == 'true'` guard as
`test`, and runs in this order:

1. `actions/checkout@v4`
2. `docker compose -f docker-compose.yml -f docker-compose.sbx.yml up -d jellyfin-sandbox`
3. `... run --rm sandbox-generate`
4. `... run --rm sandbox-provision` (mints `.env.sbx` + `config.sbx.json`)
5. `... run --rm sandbox-reset`
6. `... up -d` (full stack: jellyfin-sandbox + server + client + proxy) —
   only after step 4 has produced the generated env/config, per a comment in
   the workflow explaining this ordering.
7. A polling loop (`for i in $(seq 1 60); do ...; sleep 5; done`) reading
   `docker compose ... ps --format json client | jq -r '.Health // .State'`
   until `"healthy"`, else fails after ~5 minutes and dumps client logs — no
   fixed sleep as the sole wait strategy.
8. `... run --rm -e PROOF_URL=http://proxy:8080 -e PROOF_FLOW=all proof` — the
   all-flows mode from AC1/AC2 through the same-origin proxy target.

Builder ran this exact sequence for real locally (from-scratch worktree, no
prior sandbox state): jellyfin-sandbox up, sandbox-generate,
sandbox-provision (users Alice/Bob/Carol/Dave, libraries, scan, API key
mint, `.env.sbx` + `config.sbx.json` written), sandbox-reset, full stack up,
client healthcheck confirmed `"healthy"` via direct `docker compose ps
--format json client` inspection matching the poll script's exact check.

## Verdict

Sufficient. Checked.
