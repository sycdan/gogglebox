# Proof — AC6: `timeout-minutes: 60` on the `e2e` job

## Criterion

The `e2e` job runs under `timeout-minutes: 60`, documented in the workflow
comment as an initial tunable value covering sandbox bootstrap plus 14
sequential Playwright flows against the sandbox stack rather than a
permanent constant.

## Evidence

Cross-checked directly against `.github/workflows/publish.yml` (lines 92-96):

```yaml
    # Initial tunable value, not a measured permanent constant: covers sandbox
    # bootstrap (Jellyfin first-run wizard + fixture generation/provisioning)
    # plus all sequential Playwright flows against the sandbox stack. Tighten or
    # raise once real CI timing is known.
    timeout-minutes: 60
```

`timeout-minutes: 60` is present on the `e2e` job, immediately preceded by a
comment explicitly documenting it as an initial tunable, not a permanent
constant — matching the AC's literal wording.

## Verdict

Sufficient. Checked.
