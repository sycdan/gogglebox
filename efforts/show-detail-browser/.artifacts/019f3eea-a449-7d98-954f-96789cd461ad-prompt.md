---
prompt_id: 019f3eea-a449-7d98-954f-96789cd461ad
target_agent: gogglebox-prover
effort_path: efforts/show-detail-browser/ShowDetailBrowser.md
output_path: efforts/show-detail-browser/.artifacts/019f3eea-aa0d-7fac-8ca1-b1f85cee8481-output.md
base_tag: handoff/019f3eea-a449-7d98-954f-96789cd461ad
session_name: show-detail-browser.1783466994872
---

First, `cd` into `./sessions/show-detail-browser.1783466994872` before doing
anything else. Make all edits, commands, and commits from there.

## Effort

`efforts/show-detail-browser/ShowDetailBrowser.md` — Show Detail Browser. The
builder already implemented and the verifier already passed typecheck/tests
for all four acceptance criteria on `main`. Your job is visual proof.

## Task

Bring up the **sbx** stack (seeded, offline sandbox Jellyfin — real-ish data,
deterministic) and drive the Playwright suite to screenshot all four
acceptance criteria:

1. Clicking a show title anywhere it appears (media card, continue-watching
   card) opens an accessible show modal without breaking the page state
   behind it.
2. The modal lists episodes groupable/filterable by season; selecting a
   season button narrows the visible episode list.
3. Each visible episode row shows watched/unwatched state for every watcher
   in the active group.
4. Jellyfin search inside the modal finds episodes by keyword scoped to that
   one show only — no cross-show results, not a global/discovery-rail search.

Use `./scripts/sbx.sh up -d` to bring up the stack, then drive proof via the
`e2e/run.mjs` harness (add a flow under `e2e/flows/` if one doesn't already
exist for this feature — read `kb/00000000-0000-0000-0000-000000000000.md`'s
"Visual proof" section for the harness conventions, `PROOF_RUN_ID` grouping,
and same-origin proxy requirements). Set the same `PROOF_RUN_ID` across
invocations if you need more than one flow run.

## Output — proof docs

Write one proof doc per acceptance criterion at these exact paths (already
linked from the effort spec):

- `efforts/show-detail-browser/.artifacts/019f2aa8-4939-7df8-b837-de245fa41849-proof.md` (AC1: modal opens, page state preserved)
- `efforts/show-detail-browser/.artifacts/019f2aa8-493a-7ef9-958a-cfcc7486cdd8-proof.md` (AC2: season grouping/filtering)
- `efforts/show-detail-browser/.artifacts/019f2aa8-493c-7830-b953-38a8a7ed72ab-proof.md` (AC3: per-watcher watched state)
- `efforts/show-detail-browser/.artifacts/019f2aa8-493e-7583-8889-4e701cc2bc20-proof.md` (AC4: scoped keyword search)

Each proof doc must embed/reference screenshots **copied into this effort's
`.artifacts/` directory** (not bare `./artifacts`, which is gitignored and
will not survive session teardown — copy the relevant PNGs in from
`./artifacts/<PROOF_RUN_ID>/...` before writing the doc).

Also write your overall run summary (what you drove, what stack/data you
used, any flakiness or gaps) to
`efforts/show-detail-browser/.artifacts/019f3eea-aa0d-7fac-8ca1-b1f85cee8481-output.md`
(no frontmatter needed).

You are read-only on `src/`. If you find the feature doesn't actually satisfy
a criterion, do not fix it — report exactly what's wrong in your output file
instead of writing a proof doc claiming it passed.
