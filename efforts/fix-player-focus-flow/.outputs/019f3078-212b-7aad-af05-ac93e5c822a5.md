# Output: populate effort spec for fix-player-focus-flow

## Status: done

Replaced the stub at `efforts/fix-player-focus-flow/FixPlayerFocusFlow.md`
with a complete spec (Overview, Goals, Nongoals, Acceptance Criteria), based
on:
- `e2e/flows/player-focus.mjs` (full read — the page-wide `.or()` locator bug)
- `e2e/flows/player-uat.mjs` (full read — the per-card-scoped fix pattern it
  already adopted, lines ~85-108)
- `efforts/release-e2e-gate/.outputs/019f301a-1c2b-75ac-be64-a8f5e4d4ff49.md`
  (confirmed repro record: identical failure/exit in both all-flows and
  standalone `PROOF_FLOW=player-focus` runs against the same sandbox state)

## Acceptance criteria written

1. `[proof](./.proofs/019f307b-9663-7ef8-b19e-00344091e9f9.md)` — the locator
   fix itself: rewrite `player-focus.mjs`'s Play/Resume lookup to scope to a
   single chosen `.media-card` at a time (mirroring `player-uat.mjs`'s
   `nth(index)` iteration), replacing the page-wide `.or()`, with no change
   to any assertion made after the button is located/clicked (dialog focus,
   scroll lock, iframe mount, Space no-op, Escape close/scroll-restore).
2. `[proof](./.proofs/019f307b-b4d9-7c81-b3c3-32683aa3ffea.md)` —
   verification against the sandbox stack (`PROOF_FLOW=player-focus
   ./scripts/sbx.sh run --rm proof`) in both the clean/single-resumable-card
   state and a multi-resumable-card state (e.g. after
   `PROOF_FLOW=mark-all-watched`), confirming the flow now passes reliably
   in both and the prior strict-mode ambiguity no longer occurs.

## Note on UUIDv7 generation

The planning subagent had no shell/Bash tool available and hand-constructed
placeholder UUIDs. The orchestrator regenerated both proof-link UUIDs with
the actual `docker compose run --rm check npm run gen:uuid7 --silent` command
after this handoff completed, and updated both the spec's proof links and
this file to the real values shown above.

## Open questions / assumptions

- Assumed `mark-all-watched` (an existing flow name visible in the
  release-e2e-gate all-flows dispatch order) is an acceptable way to seed
  multiple simultaneously-resumable cards for AC2's multi-card verification,
  per the handoff prompt's own suggestion. Any other way of producing more
  than one visible Play/Resume button on the page would equally satisfy the
  criterion's intent.
- No subeffort was created; this is scoped as a single flat effort with two
  acceptance criteria (fix, then verification), matching the two-part
  criteria structure requested in the handoff prompt.
