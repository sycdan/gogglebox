---
prompt_id: 019f357d-bec1-72ae-9c88-0c077434b8fd
target_agent: gogglebox-builder
effort_path: efforts/fix-player-focus-flow/FixPlayerFocusFlow.md
output_path: efforts/fix-player-focus-flow/.artifacts/019f357d-d89e-7ef3-8e30-b513a80bcb1d-output.md
base_tag: handoff/019f357d-bec1-72ae-9c88-0c077434b8fd
session_name: fix-player-focus-flow.1783308868350
---

First, `cd` into `./sessions/fix-player-focus-flow.1783308868350` and do all
edits, commands, and commits from there.

## Effort

Read `efforts/fix-player-focus-flow/FixPlayerFocusFlow.md` in full —
Overview, Goals, Nongoals, and both Acceptance Criteria are already fully
specified. Also read `e2e/flows/player-focus.mjs` (the buggy flow) and
`e2e/flows/player-uat.mjs` lines ~85-108 (the already-fixed sibling flow
whose per-card-scoped locator pattern you're mirroring).

## Your phase: implementation + verification

Implement both acceptance criteria:

1. Rewrite `player-focus.mjs`'s Play/Resume button lookup to scope the search
   to a single chosen `.media-card` at a time (mirroring `player-uat.mjs`'s
   `nth(index)` iteration picking the first card whose scoped `button`
   locator matches `/^(Play|Resume)$/`), replacing the page-wide `.or()`
   locator. Do not change any assertion the flow makes after the button is
   located/clicked (dialog focus, scroll lock, iframe mount, Space no-op,
   Escape close/scroll-restore) — those must remain byte-for-byte identical
   in behavior.
2. Verify the fixed flow against the running sandbox stack
   (`PROOF_FLOW=player-focus ./scripts/sbx.sh run --rm proof`) in both: (a)
   the clean/default sandbox state with a single resumable card, and (b) a
   state with multiple simultaneously-resumable cards (e.g. run
   `PROOF_FLOW=mark-all-watched` first, or otherwise seed more than one
   visible Play/Resume button), confirming the flow passes reliably in both
   and the prior strict-mode ambiguity no longer occurs.

Respect the effort's Nongoals: don't change what the flow verifies beyond the
button-locating step; don't touch `player-uat.mjs`, `e2e/run.mjs`, sandbox
provisioning, or any other flow; don't add new keyboard-safety assertions or
otherwise expand the flow's purpose.

You may edit `e2e/flows/player-focus.mjs` only (plus bring up/use the sandbox
stack to verify). Do not touch other effort directories under `./efforts`.

## Output

Write your summary (what changed, both AC's pass/fail status with the actual
proof-run output for the single-card and multi-card cases, anything
incomplete) to
`efforts/fix-player-focus-flow/.artifacts/019f357d-d89e-7ef3-8e30-b513a80bcb1d-output.md`.
Do not edit the effort spec's checkboxes yourself — that is the approver's job.
