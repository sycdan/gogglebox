---
prompt_id: 019f3592-1db5-7628-b930-d3e0f59081eb
target_agent: gogglebox-verifier
effort_path: efforts/fix-player-focus-flow/FixPlayerFocusFlow.md
output_path: efforts/fix-player-focus-flow/.artifacts/019f3592-390c-7b33-97a9-ae77cf14c5ac-output.md
base_tag: handoff/019f3592-1db5-7628-b930-d3e0f59081eb
session_name: fix-player-focus-flow.1783310203789
---

First, `cd` into `./sessions/fix-player-focus-flow.1783310203789` and run all
commands from there.

## Effort

Read `efforts/fix-player-focus-flow/FixPlayerFocusFlow.md` in full. The fix
already landed on `main`
(`efforts/fix-player-focus-flow/.artifacts/019f357d-d89e-7ef3-8e30-b513a80bcb1d-output.md`
is the builder's own account: `e2e/flows/player-focus.mjs`'s Play/Resume
lookup was rewritten to scope to a single `.media-card` at a time via
`nth(index)`, mirroring `e2e/flows/player-uat.mjs`'s existing pattern,
replacing the old page-wide `.or()` locator).

## Your phase: independent verification + proof authoring

You are read-only on source (do not edit `e2e/` or anything else under
`src/`). Do the following:

1. Read `e2e/flows/player-focus.mjs` and confirm the Play/Resume button
   lookup is now scoped per-card (no page-wide `.or()` locator remains), and
   confirm no other assertion in the file changed (dialog focus, scroll
   lock, iframe mount, Space no-op, Escape close/scroll-restore should all
   read identically to how they behaved before this fix).
2. Bring up the sandbox stack and independently run
   `PROOF_FLOW=player-focus ./scripts/sbx.sh run --rm proof` against the
   clean/default state (single resumable card) — confirm it passes.
3. Run `PROOF_FLOW=mark-all-watched ./scripts/sbx.sh run --rm proof` to seed
   multiple simultaneously-resumable cards, then run
   `PROOF_FLOW=player-focus ./scripts/sbx.sh run --rm proof` again against
   that state — confirm it passes with no Playwright strict-mode ambiguity
   error.
4. Run `docker compose run --rm check` and `docker compose run --rm test`
   fresh — confirm no regression (prior baseline: 150 tests, 146 pass, 4
   pre-existing real-Jellyfin skips, 0 fail).

Note: if port 8080 collides with another running stack, do not stop a
container you don't own — use a temporary untracked compose override to
republish on a different host port for your own verification runs only, and
delete it afterward (this is what the builder phase did; see its output for
the exact pattern).

Write two proof docs (create files via shell redirection/heredoc) citing what
you found in steps 1-4, including actual command output/exit codes:

- `efforts/fix-player-focus-flow/.artifacts/019f307b-9663-7ef8-b19e-00344091e9f9-proof.md`
  (AC1 — the locator rewrite itself)
- `efforts/fix-player-focus-flow/.artifacts/019f307b-b4d9-7c81-b3c3-32683aa3ffea-proof.md`
  (AC2 — passes in both single-card and multi-card states)

Flag anything insufficient rather than fixing it.

## Output

Write your pass/fail verdict and findings to
`efforts/fix-player-focus-flow/.artifacts/019f3592-390c-7b33-97a9-ae77cf14c5ac-output.md`.
