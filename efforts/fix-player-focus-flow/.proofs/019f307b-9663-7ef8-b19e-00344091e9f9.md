# AC1 proof — locator rewrite scoped per-card, no other assertion changed

Independently verified by gogglebox-verifier (read-only on source).

## What was checked

`git diff main HEAD -- e2e/flows/player-focus.mjs` is empty: this worktree's
HEAD is identical to `main`'s HEAD (`0d8f354`), confirming the builder's fix
already landed and there is no drift to re-review separately.

`git diff c4a1372 HEAD -- e2e/flows/player-focus.mjs` (pre-fix commit vs now)
shows the only change is:

```diff
-  const playButton = page
-    .locator('.media-card button', { hasText: /^Play$/ })
-    .first()
-    .or(page.locator('.media-card button', { hasText: /^Resume$/ }).first());
+  const cardCount = await page.locator('.media-card').count();
+  let playButton = null;
+  for (let index = 0; index < cardCount; index += 1) {
+    const card = page.locator('.media-card').nth(index);
+    const button = card.locator('button', { hasText: /^(Play|Resume)$/ }).first();
+    if (await button.count().then((n) => n > 0)) {
+      playButton = button;
+      break;
+    }
+  }
+
+  if (!playButton) {
+    await shootView(page, `${flowName}-03-no-play-button`);
+    fail('player-focus: found no Play/Resume button to open the modal');
+  }
```

The page-wide `.or()` locator is gone. The replacement scopes the search to
one `.media-card` at a time via `page.locator('.media-card').nth(index)`,
then a card-scoped `button` locator matching `/^(Play|Resume)$/`, picking the
first card whose scoped locator has a match — this mirrors
`e2e/flows/player-uat.mjs` lines ~85-101 exactly (same `nth(index)` loop,
same card-scoped `button` locator with the same combined regex).

No other line in the file changed. Confirmed by reading the full file
(`e2e/flows/player-focus.mjs`, 149 lines) end to end: the dialog-focus
assertion (`after.isDialog` / `after.isPlayButton`), the scroll-lock
assertion (`after.overflow !== 'hidden'`), the iframe-mount assertion
(`after.iframeSrc` must include `/player/`), the Space no-op assertions
(`dialogCount`, `activeIsDialog`, `scrollY` unchanged), and the Escape
close/scroll-restore assertions (`dialog` detaches, `overflowAfterClose`
not `'hidden'`) are all textually identical to the pre-fix version — only
the button-locating step and its own new "no button found" guard were
touched.

## Verdict

PASS. Locator rewrite matches the effort's AC1 wording exactly; no
downstream assertion was altered.
