# AC2 proof — passes in both single-card and multi-card sandbox states

Independently verified by gogglebox-verifier against a freshly bootstrapped
sandbox stack in this worktree (`fix-player-focus-flow.1783310203789`, which
had no sandbox provisioned yet, so `up -d jellyfin-sandbox`, `run --rm
sandbox-generate`, `run --rm sandbox-provision` were run first as standard
one-time setup).

Port 8080 was already bound by another worktree's long-running
`gogglebox-proxy-1`. Per the handoff instructions, a temporary untracked
`docker-compose.local-port-override.yml` (republishing `proxy` on host 8081
via `ports: !override`) was created for this verification's own runs only,
and deleted immediately after (`git status --short` before and after
confirms no tracked or leftover untracked files). `player-focus` itself
never depends on the host-published port — it drives Playwright against
`http://client:5173` inside the docker network — so this override only
avoided a host-port collision, nothing else.

## (a) Clean/default sandbox state — single resumable card

`PROOF_FLOW=player-focus ./scripts/sbx.sh run --rm proof` (with the local
port override layered on):

```
[proof] player-focus: locating main app
[proof] player-focus: selecting a viewer party
[proof] player-focus: activeElement BEFORE open = {"tag":"BUTTON","text":"Play"}
[proof] player-focus: activeElement AFTER open = {"tag":"DIV","role":"dialog","cls":"modal player-modal","isDialog":true,"isPlayButton":false,"overflow":"hidden","iframeSrc":"http://client:5173/player/web/index.html#/details?id=89e7776f02b119c19ec79c4eed30861f&play=true&autoplay=true&serverId=d93ee3b4bd9c463686449bd66bab2e84"}
[proof] player-focus: AFTER Space = {"dialogCount":1,"activeIsDialog":true,"scrollY":0}
[proof] player-focus: document.body.style.overflow after close = ""
[proof] player-focus: PASS - dialog focus, iframe mount, Space safety, and Escape close all work
[proof] OK
```

Exit: OK (single run, no ambiguity possible — one resumable card on the
freshly provisioned rail).

## (b) Multi-resumable-card state

Seeded via `PROOF_FLOW=mark-all-watched ./scripts/sbx.sh run --rm proof`,
which passed on its own (`[proof] mark-all-watched: flow complete` /
`[proof] OK`) and left three simultaneously-resumable cards on the
"Continue watching" rail, confirmed visually in
`artifacts/2026-07-06T04-03-31-922Z/player-focus-03-before-open.png`:
"Finale" (Near Finale S01E03, 0%, Play), "Return" (Normal Show S02E01, 0%,
Play), "Departure" (Specials Show S01E03, 0%, Play) — three visible Play
buttons at once, exactly the ambiguous-strict-mode scenario the old `.or()`
locator failed on.

`PROOF_FLOW=player-focus ./scripts/sbx.sh run --rm proof` was then run
**twice** in this state to rule out flakiness:

Run 1:
```
[proof] player-focus: activeElement BEFORE open = {"tag":"BUTTON","text":"Play"}
[proof] player-focus: activeElement AFTER open = {"tag":"DIV","role":"dialog","cls":"modal player-modal","isDialog":true,"isPlayButton":false,"overflow":"hidden","iframeSrc":"http://client:5173/player/web/index.html#/details?id=7d79e099db9df54afd7b2ded252fdf70&play=true&autoplay=true&serverId=d93ee3b4bd9c463686449bd66bab2e84"}
[proof] player-focus: AFTER Space = {"dialogCount":1,"activeIsDialog":true,"scrollY":0}
[proof] player-focus: document.body.style.overflow after close = ""
[proof] player-focus: PASS - dialog focus, iframe mount, Space safety, and Escape close all work
[proof] OK
```

Run 2: identical output (same chosen card id `7d79e099db9df54afd7b2ded252fdf70`,
same PASS line), exit OK.

No Playwright strict-mode "resolved to N elements" ambiguity error occurred
in either state across all three total `player-focus` invocations (1
clean + 2 multi-card).

## Verdict

PASS. The fix holds in both the single-card and multi-card states, and is
stable across repeated runs of the multi-card state.
