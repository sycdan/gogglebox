---
prompt_id: 019f33e2-454b-75c2-a24a-1c9395391e78
target_agent: gogglebox-verifier
effort_path: efforts/rename-group-to-party/RenameGroupToParty.md
output_path: efforts/rename-group-to-party/.outputs/019f33e2-5c9b-73fc-8082-3ec70b50e197.md
base_tag: handoff/019f33e2-454b-75c2-a24a-1c9395391e78
session_name: rename-group-to-party.1783281901262
---

First, `cd` into `./sessions/rename-group-to-party.1783281901262` and do all
commands from there.

## Effort

Read `efforts/rename-group-to-party/RenameGroupToParty.md` in full. The
group->party rename implementation (AC1-4) already landed on `main` in commit
`383f5eb` (builder output at
`efforts/rename-group-to-party/.outputs/019f33b8-bcab-7de3-9b20-ccc8bf0dfc57.md`
— read it for what changed: new `/api/party*` routes and `parties.ts`/
`partyKey.ts` with `group`-compat aliases, config migration dual-spelling
`groups[]`/`parties[]`, renamed e2e flows `party-alias.mjs`/`party-pin.mjs`,
updated docs).

## Your phase: static verification (part of AC5)

AC5 requires "automated verification covers the terminology migration,
including typecheck, unit coverage for config/API compatibility, and at least
one visual proof that the party language appears in the running UI." You are
covering the typecheck + unit coverage part only — visual proof is a separate
later runtime/prover phase.

Run `docker compose run --rm check` (typecheck) and
`docker compose run --rm test` (unit tests) and report pass/fail with failing
snippets if any. Specifically confirm unit coverage exists for: config
migration accepting both legacy `groups[]` and new `parties[]` config shapes
(`src/server/configMigrations.test.ts`, `src/server/config.test.ts`), and
API/state compatibility between group- and party-oriented fields/routes
(`src/server/parties.test.ts`, `src/server/partyKey.test.ts`,
`src/server/appState.test.ts`, `src/server/accounts.test.ts`). Flag anything
missing or weak — you are read-only on source, report findings rather than
fixing them.

## Output

Write your pass/fail verdict and findings to
`efforts/rename-group-to-party/.outputs/019f33e2-5c9b-73fc-8082-3ec70b50e197.md`.
