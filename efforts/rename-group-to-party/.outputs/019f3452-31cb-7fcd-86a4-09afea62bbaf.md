---
output_id: 019f3452-31cb-7fcd-86a4-09afea62bbaf
role: gogglebox-approver
prompt: .prompts/019f3452-15f8-7c18-8814-09a44b851319.md
---

# Rename Group To Party — approval

## Status: pass

`efforts/rename-group-to-party/RenameGroupToParty.md` — all 5 acceptance
criteria checked.

## Checked

1. **AC1** — user-visible copy/nav/labels/empty-states/proof-test-flow text
   says "party". Proof:
   `.proofs/019f2aa8-4912-7399-87c4-c849a4287ff5.md`. Read all 11 referenced
   PNGs directly from `.proofs/` (durable, in-tree, not `./artifacts`):
   `party-alias-picker.png`, `party-pin-picker.png`, `party-alias-saved.png`,
   `party-alias-reused.png`, `party-alias-created.png`,
   `party-pin-success.png`, `party-pin-prompt.png`, `party-pin-wrong.png`,
   `party-pin-mixed-warning.png`, `party-pin-login.png`,
   `party-pin-invalid-token.png`. Every rendered screenshot matched the
   claimed copy exactly ("Pick the party" heading, "SAVED PARTIES" list,
   "PARTY PICKS" recommendation rail, "Carol's PIN" field label, "Incorrect
   or missing pin for Carol." error, "This party includes viewers beyond the
   default household set..." mixed-tier warning). No leftover "group" text
   visible anywhere. Sufficient.

2. **AC2** — server API contracts/client state use party-oriented names,
   with group-oriented compatibility maintained. Proof:
   `.proofs/019f2aa8-4914-78bb-bbc1-c712d449056d.md`. Cross-checked the cited
   `src/server/server.ts` line ranges directly (routes at 379-467,
   `/api/session` at 266-291): every citation was accurate — canonical
   `/api/party*` routes are registered immediately followed by
   `// Pre-rename compatibility alias` comments and identical `/api/group*`
   routes wired to the same handler functions; `GET /api/session` computes
   `partyAlias` once and returns it under both `activePartyAlias` and
   `activeGroupAlias`; `handleListParties` returns both `parties` and a
   `groupKey`-remapped `groups` array from the same computed list. The proof
   doc's live curl session (fresh sandbox stack, byte-for-byte identical
   dual-route/dual-field responses) plus `appState.test.ts`'s
   legacy-key-fallback test and a fresh green `check`/`test` run support the
   claim. Sufficient.

3. **AC3** — config auto-migrates/aliases every previously-supported
   group-shaped config, no manual step, no rollback risk. Proof:
   `.proofs/019f2aa8-4915-7f3f-87f6-1133d60fcfa8.md`. Cross-checked
   `src/server/configMigrations.ts:75-124` directly: `legacyGroupsFrom`
   correctly resolves `parties[] ?? groups[]` with documented `parties[]`
   precedence, no new `schemaVersion` was introduced, matching the proof
   doc's citations exactly. Combined with `configMigrations.test.ts` and
   `config.test.ts` full-pipeline coverage (both spellings migrate to an
   identical resulting shape) and a fresh green `check`/`test` run.
   Sufficient.

4. **AC4** — sandbox/UAT/README/developer docs/automation scripts describe
   parties consistently, with compat notes for legacy group keys/paths.
   Proof: `.proofs/019f2aa8-4917-7775-b377-ac01f90308ec.md`. Cites exact
   `git show 383f5eb` diffs for README.md, tools/sandbox/README.md,
   tools/sandbox/provision.mjs, tools/proxy/Caddyfile, docker-compose.yml,
   docker-compose.sbx.yml, each with an explicit compatibility note, and
   confirms via direct `grep` that the UAT stack has no group/party
   language needing migration. A fresh sandbox provisioning run in the same
   proof phase confirmed documented account/tier behavior matches live
   automation output. Sufficient.

5. **AC5** — automated verification (typecheck, unit coverage for
   config/API compatibility, at least one visual proof). Proof:
   `.proofs/019f2aa8-4918-7f02-84c7-b04208e85468.md`. Static half: green
   `check` (0 errors) and `test` (150 tests, 146 pass, 0 fail, 4 pre-existing
   real-Jellyfin skips) cited from the verifier phase output
   (`.outputs/019f33e2-5c9b-73fc-8082-3ec70b50e197.md`) and independently
   reproduced by the builder proof-authoring phase
   (`.outputs/019f33ed-e747-75a6-9f49-725cee0ea2ac.md`) with identical
   numbers (no regression). Visual half: this phase's own fresh
   `party-alias` and `party-pin` e2e runs against a live sandbox Jellyfin,
   with all 11 screenshots verified above under AC1 reused here — both
   flows exited 0 with every internal PASS assertion logged. Sufficient.

## Remaining

None. All 5 acceptance criteria are checked. No child subefforts are
visible under `efforts/rename-group-to-party/` (only `.prompts/`,
`.outputs/`, `.proofs/` metadata directories and the spec file — no nested
effort directories).

## Evidence read

- `efforts/rename-group-to-party/RenameGroupToParty.md` — effort spec
  (top-level, no parent).
- `.proofs/019f2aa8-4912-7399-87c4-c849a4287ff5.md` (AC1) — sufficient;
  screenshots verified directly.
- `.proofs/019f2aa8-4914-78bb-bbc1-c712d449056d.md` (AC2) — sufficient;
  `server.ts` citations verified directly against source.
- `.proofs/019f2aa8-4915-7f3f-87f6-1133d60fcfa8.md` (AC3) — sufficient;
  `configMigrations.ts` citations verified directly against source.
- `.proofs/019f2aa8-4917-7775-b377-ac01f90308ec.md` (AC4) — sufficient;
  doc/script diff claims are specific and cite a real commit hash, checked
  for internal consistency against the AC1/AC2/AC3 evidence.
- `.proofs/019f2aa8-4918-7f02-84c7-b04208e85468.md` (AC5) — sufficient;
  reuses the same screenshots verified for AC1 plus cites a verifier run
  independently corroborated by a second, later fresh test run with
  identical pass/fail counts.
- `.outputs/019f33b8-bcab-7de3-9b20-ccc8bf0dfc57.md` (builder: AC1-4
  implementation) — context/history, not itself proof.
- `.outputs/019f33e2-5c9b-73fc-8082-3ec70b50e197.md` (verifier:
  typecheck/unit) — corroborates AC5's static half; also the source of the
  two flagged non-blocking gaps (see below).
- `.outputs/019f33e7-0756-7b78-a6a2-f824c565b1f7.md` (prover: visual proof
  for AC1/AC5) — corroborates the screenshot provenance/copy chain.
- `.outputs/019f33ed-e747-75a6-9f49-725cee0ea2ac.md` (builder: AC2-4 proof
  docs) — corroborates AC2/AC3/AC4 evidence-gathering methodology.
- `src/server/server.ts` (lines 260-294, 375-474) — read directly to verify
  AC2's route/field citations.
- `src/server/configMigrations.ts` (lines 75-124) — read directly to verify
  AC3's migration/alias citations.
- 11 PNGs under `efforts/rename-group-to-party/.proofs/` — read directly,
  all confirmed party terminology with no leftover "group" text.

## Missing evidence

None outstanding — all five criteria had sufficient, durable, in-tree proof.

## Non-blocking coverage gaps (flagged for visibility, not blocking)

The `gogglebox-verifier` phase
(`.outputs/019f33e2-5c9b-73fc-8082-3ec70b50e197.md`) flagged two residual
test-coverage gaps, both explicitly carried forward (not fixed) through the
later proof-authoring phases and both explicitly acknowledged as
non-blocking for AC5 as written:

1. **No golden-value regression test for `derivePartyKey`'s namespace
   constant.** `partyKey.test.ts` only asserts internal self-consistency
   (determinism, order-independence, dedup, hex format) — none of it pins a
   known/expected output hash for a fixed input. A future accidental change
   to the `PARTY_NAMESPACE` UUIDv5 constant (which must never change, per
   its own source comment, since it's baked into every previously-persisted
   key and minted Jellyfin username) would still pass every existing test.
   Recommended follow-up: add one test asserting
   `derivePartyKey(['user-a','user-b'])` equals a specific hard-coded
   32-char hex value computed today.

2. **No HTTP-route-level test for the `/api/group*` compatibility
   aliases.** There is no supertest-style test anywhere in `src/server/`
   (a pre-existing gap in the codebase's test strategy, not newly
   introduced by this effort), so the route-alias/dual-field claim in AC2
   rests on manual curl verification against a live sandbox (reproduced
   fresh in two separate proof-phase sessions with identical results) and
   direct reading of `server.ts`'s route registrations, rather than a
   CI-enforced automated test. Recommended follow-up: a lightweight
   supertest-based route test covering `/api/group` → `/api/party`,
   `/api/groups` → `/api/parties`, `/api/group/clear` → `/api/party/clear`,
   and `/api/group/verify-pins` → `/api/party/verify-pins` wiring parity.

Both gaps are read-only findings recommended for a possible future
follow-up effort; they do not block approval of AC5 as its text is
actually written ("typecheck, unit coverage for config/API compatibility,
and at least one visual proof" — all three of which are present and green).

## Effort-level conclusion

`rename-group-to-party` is **done**. All 5 acceptance criteria are proven
and checked in `RenameGroupToParty.md`. This is a top-level effort with no
parent spec and no nested subeffort directories to confirm. Checked
criteria become canonical once the orchestrator consumes this handoff onto
`main`.
