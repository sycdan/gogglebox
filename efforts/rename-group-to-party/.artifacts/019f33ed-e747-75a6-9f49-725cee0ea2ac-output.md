---
output_id: 019f33ed-e747-75a6-9f49-725cee0ea2ac
role: gogglebox-builder
prompt: .artifacts/019f33ed-cdf4-7869-b5ab-e0f611e39a05-prompt.md
---

# Rename Group To Party — proof-authoring phase (AC2, AC3, AC4)

## What I did

No new code changes — this phase was purely evidence-gathering and proof
writing, as instructed. I read the effort spec, the prior builder
implementation output, the prior verifier output, and the existing AC1/AC5
proof docs for context, then:

1. Re-read the exact server routes, session response, and client state code
   for AC2 (`src/server/server.ts` lines 132, 260-291, 370-467;
   `src/client/App.tsx` lines 157-172, 421, 778-947, 1599-1601).
2. Re-read `src/server/configMigrations.ts` in full and the AC3-relevant
   tests in `src/server/configMigrations.test.ts` (lines 95-145) and
   `src/server/config.test.ts` (lines 123-184).
3. Confirmed the exact AC4 doc/script diffs via `git show 383f5eb -- <path>`
   for `README.md`, `tools/sandbox/README.md`, `tools/sandbox/provision.mjs`,
   `tools/proxy/Caddyfile`, `docker-compose.yml`, `docker-compose.sbx.yml`,
   and cross-checked the UAT stack (`docker-compose.uat.yml`, `scripts/uat.sh`)
   has no group/party language to migrate.
4. Ran `docker compose run --rm check` (clean) and `docker compose run --rm
   test` (150 tests, 146 pass, 0 fail, 4 skipped — same numbers as the prior
   verifier phase, confirming no regression) fresh in this session.
5. Provisioned a brand-new sandbox stack from scratch (`./scripts/sbx.sh up
   -d jellyfin-sandbox` → `sandbox-generate` → `sandbox-provision` → `up
   -d`) and drove the running server directly with `curl` as both the
   `household` and `visitor` accounts, proving live that:
   - `POST /api/group` (legacy) and `POST /api/party` (new) both form a
     party via the same handler.
   - `GET /api/session` returns `activePartyAlias`/`activeGroupAlias` with
     identical values.
   - `GET /api/parties` and `GET /api/groups` return byte-for-byte identical
     dual-shaped bodies (`partyKey`/`groupKey` sharing the same hash).
   - `POST /api/party/clear` and `POST /api/group/clear` both fully reset
     the active party.
   - `POST /api/group/verify-pins` (wrong pin) returns 403 and `POST
     /api/party/verify-pins` (correct pin) returns 200 — same validation
     logic on both routes.
   - The sandbox's generated `.env.sbx`/`config.sbx.json` matched
     `tools/sandbox/README.md`'s documented account/tier description
     exactly.
6. Tore the sandbox stack down (`./scripts/sbx.sh down`) after gathering
   evidence; confirmed `git status` shows only the three new proof files,
   no stray generated artifacts.

## Proof docs written

- `efforts/rename-group-to-party/.artifacts/019f2aa8-4914-78bb-bbc1-c712d449056d-proof.md`
  (AC2) — cites the paired route registrations in `server.ts`, the dual
  `activePartyAlias`/`activeGroupAlias` and `parties`/`groups` response
  shapes, the client's exclusive use of the new names, and the full live
  curl session against a freshly provisioned sandbox proving old and new
  routes/fields are wired to identical handlers and agree byte-for-byte.
  Also carries forward the verifier's flagged gap (no automated HTTP-route
  test exists for any endpoint in this codebase) as a documented residual
  risk, not fixed in this phase.
- `efforts/rename-group-to-party/.artifacts/019f2aa8-4915-7f3f-87f6-1133d60fcfa8-proof.md`
  (AC3) — cites `configMigrations.ts`'s `legacyGroupsFrom` alias resolution
  (parties[] wins, groups[] still works, no new schemaVersion), the four
  targeted `configMigrations.test.ts` cases (existing groups[] case still
  passes, new parties[] case, precedence case, warning-language case), and
  the two full-pipeline `config.test.ts` cases proving `groups[]` and
  `parties[]` configs migrate to an identical resulting effective-config
  shape end-to-end.
- `efforts/rename-group-to-party/.artifacts/019f2aa8-4917-7775-b377-ac01f90308ec-proof.md`
  (AC4) — cites the exact `git show 383f5eb` diffs for each named
  README/doc/script file, confirms the compatibility notes are present on
  the current worktree (not just historical diff), confirms the UAT stack
  has no group/party language needing migration, and confirms the live
  sandbox provisioning output matches the documented description.

## Code changes

None. I found no actual gaps requiring a code fix while gathering evidence —
the implementation from the prior builder phase held up under fresh
`check`/`test`/live-curl verification with no discrepancies.

## Anything incomplete / left for later phases

- I did not touch the effort spec's checkboxes (approver's job) or any other
  effort directory.
- The verifier-flagged gaps (no golden-value regression test for
  `derivePartyKey`'s namespace constant; no HTTP-route-level automated test)
  remain open, documented residual risks — carried forward in the AC2 proof
  doc, not addressed in this phase since the handoff scoped this phase to
  proof-authoring only and these are pre-existing test-strategy gaps, not
  new breakage.
