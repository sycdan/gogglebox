---
criterion: AC1 — all user-visible application copy, navigation, form labels, empty states, and proof/test flow text refer to parties instead of groups while preserving the existing workflows
proved_by: gogglebox-prover
---

# AC1 proof — party terminology in the running UI

Ran the renamed e2e proof flows (`party-alias`, `party-pin`) against a
freshly provisioned sandbox Jellyfin stack under a shared
`PROOF_RUN_ID=rename-group-to-party-prove`. Both flows exited 0 (PASS on
every internal assertion). Screenshots were copied from
`./artifacts/rename-group-to-party-prove/...` into this `.artifacts/` directory
so they survive session teardown; the descriptions below are of the
in-tree copies, and every one was Read directly to confirm its content.

## Viewer-picker heading / nav (was "Pick the group")

- [`party-alias-picker.png`](./party-alias-picker.png) — headline "Pick the
  party" over the "WHO IS WATCHING?" eyebrow, viewer cards for Alice/Bob/
  Carol/Dave, "Continue" button.
- [`party-pin-picker.png`](./party-pin-picker.png) — same "Pick the party"
  heading for the visitor-token session, showing only Dave plus an
  "Add guest" card (party membership correctly scoped per account tier).

## Saved-party empty/list state (was "Saved groups")

- [`party-alias-saved.png`](./party-alias-saved.png) — a "SAVED PARTIES"
  section header above a saved party card labeled "Alice + Bob" with member
  names "Alice, Bob" underneath.
- [`party-alias-reused.png`](./party-alias-reused.png) — confirms selecting
  that saved party and continuing returns to the main app without minting a
  duplicate saved entry (workflow preserved across the rename).

## Main app header / recommendation rail (was "Group picks")

- [`party-alias-created.png`](./party-alias-created.png) — top bar shows
  "Alice + Bob" as the active-party alias next to "Ignored", "Change
  viewers", "Log out"; a recommendation rail eyebrow reads "PARTY PICKS"
  above "Because none of you have seen this".
- [`party-pin-success.png`](./party-pin-success.png) — same layout for the
  guest-PIN party (active alias "Carol", "PARTY PICKS" rail still present).

## Guest/PIN form labels and validation copy (was group-oriented pin copy)

- [`party-pin-prompt.png`](./party-pin-prompt.png) — "GUESTS" / "Enter guest
  PINs" modal, field labeled "Carol's PIN".
- [`party-pin-wrong.png`](./party-pin-wrong.png) — inline validation error
  "Incorrect or missing pin for Carol." after submitting a wrong PIN; modal
  stays open, no party formed (workflow preserved).
- [`party-pin-mixed-warning.png`](./party-pin-mixed-warning.png) — "HEADS UP
  / Shared watch progress" dialog: "This party includes viewers beyond the
  default household set. Continuing will affect watch progress and watched
  states for ALL users in the party." — the mixed-selection warning
  explicitly uses "party" language.

## Login screen (unaffected copy, included for chain-of-custody)

- [`party-pin-login.png`](./party-pin-login.png) and
  [`party-pin-invalid-token.png`](./party-pin-invalid-token.png) — the
  "LAN HOUSEHOLD PORTAL" / "Gogglebox" access-token screen and its "Invalid
  access token" error. This screen never mentioned "group"; shown to confirm
  the party screens above were reached via a genuine token round-trip, not a
  stubbed/mocked session.

## Cross-check for leftover "group" text

Across all eleven screenshots I read, no visible heading, button, label,
empty-state, or validation message used the word "group" — every
user-facing instance says "party"/"parties" (picker heading, saved-party
list, recommendation rail, guest PIN prompt/error, and the mixed-selection
warning). This matches the builder's claimed copy changes in
`efforts/rename-group-to-party/.artifacts/019f33b8-bcab-7de3-9b20-ccc8bf0dfc57-output.md`
("Pick the group" → "Pick the party", "Saved groups" → "Saved parties",
"Group picks" → "Party picks", mixed-selection warning now says
"party"/"parties").

## Verdict

**Proved.** The running UI, driven through real e2e flows against a live
sandbox Jellyfin, shows party terminology consistently across navigation,
headings, form labels, and empty/validation states, with existing workflows
(create/save/reuse a party, add a guest by PIN, reject a wrong PIN, warn on
mixed-tier parties) all still functioning end-to-end.
