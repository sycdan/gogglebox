---
prompt_id: 019f33b8-a312-742d-ba92-2048f426bf59
target_agent: gogglebox-builder
effort_path: efforts/rename-group-to-party/RenameGroupToParty.md
output_path: efforts/rename-group-to-party/.artifacts/019f33b8-bcab-7de3-9b20-ccc8bf0dfc57-output.md
base_tag: handoff/019f33b8-a312-742d-ba92-2048f426bf59
session_name: rename-group-to-party.1783279181575
---

First, `cd` into `./sessions/rename-group-to-party.1783279181575` and do all
edits, commands, and commits from there.

## Effort

Read `efforts/rename-group-to-party/RenameGroupToParty.md` in full before
starting. This is a terminology/compatibility rename of "group" to "party"
throughout Gogglebox, not a data-model redesign.

## Your phase: implementation

You are implementing acceptance criteria 1-4:

1. All user-visible application copy, navigation, form labels, empty states,
   and proof/test flow text refer to parties instead of groups, while
   preserving existing workflows.
2. Server API contracts and client state use party-oriented names for newly
   exposed fields and helpers, with compatibility maintained for any existing
   group-oriented consumers or persisted state.
3. Config loading auto-migrates or aliases every previously supported
   group-shaped config to party terminology without a manual migration step or
   schema rollback risk.
4. Sandbox, UAT, README, developer docs, and relevant automation scripts
   describe parties consistently, while retaining compatibility notes where
   operators may still see legacy group keys or paths.

Respect the effort's Nongoals: do not change how parties are selected,
authenticated, authorized, or mapped to Jellyfin users; do not split, merge, or
redesign the group data model beyond the naming migration; do not remove
backward compatibility for existing `group` config fields, database fields,
routes, or persisted client state — extend the `schemaVersion` migration chain
in `src/server/config.ts` so any older config auto-migrates forward, per this
repo's standing convention (see `kb/00000000-0000-0000-0000-000000000000.md`).

You may edit anything under `src/`, `e2e/`, `tools/`, docs (`README.md`,
developer docs), and automation scripts, plus commit inside your session
worktree/branch. Do not touch other effort directories under `./efforts`. Use
`docker compose run --rm check` / `docker compose run --rm test` inside the
worktree to self-check as you go (full verification is a later phase, but do
not hand off obviously broken code).

Acceptance criterion 5 (typecheck/unit/visual proof) is out of scope for this
phase — a later verifier/runtime/prover phase covers it.

## Output

When your implementation is ready for review, write a summary of what changed,
which of AC1-4 you believe are satisfied and why, any compatibility aliases you
added, and anything you were unable to complete, to
`efforts/rename-group-to-party/.artifacts/019f33b8-bcab-7de3-9b20-ccc8bf0dfc57-output.md`.
Do not edit the effort spec's checkboxes yourself — that is the approver's job.
