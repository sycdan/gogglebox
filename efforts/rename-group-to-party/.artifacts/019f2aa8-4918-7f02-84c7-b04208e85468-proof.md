---
criterion: AC5 — automated verification covers the terminology migration (typecheck, unit coverage for config/API compatibility, at least one visual proof)
proved_by: gogglebox-prover
---

# AC5 proof — verification chain (typecheck + unit + visual)

## Static verification (typecheck + unit tests)

Cited from `efforts/rename-group-to-party/.artifacts/019f33e2-5c9b-73fc-8082-3ec70b50e197-output.md`
(gogglebox-verifier phase, run against this same rename):

| command | exit | result |
|---|---|---|
| `docker compose run --rm check` | 0 | `tsc -p tsconfig.json --noEmit && tsc -p tsconfig.server.json --noEmit` — no errors |
| `docker compose run --rm test` | 0 | 150 tests, 146 pass, 0 fail, 4 skipped (pre-existing real-Jellyfin tests, expected without a live Jellyfin) |

That phase confirmed targeted unit coverage for the config/API-compatibility
claim in AC3, specifically:
- `src/server/configMigrations.test.ts` and `src/server/config.test.ts` prove
  legacy `groups[]` and new `parties[]` config spellings both migrate through
  the same schemaVersion chain to an identical resulting shape, with
  `parties[]` taking precedence when both are present.
- `src/server/appState.test.ts` proves persisted state falls back to reading
  legacy `groupAliases`/`groupPlayerUsers` keys and normalizes forward to
  `partyAliases`/`partyPlayerUsers` on next write, with zero data loss.
- `src/server/parties.test.ts`, `src/server/partyKey.test.ts`,
  `src/server/accounts.test.ts` cover the renamed API surface
  (`buildPartyAlias`, `derivePartyKey`, `verifyPartyPins`,
  `resolvePartyMemberSelection`, etc.) under their new names.

That verifier output also flagged two residual gaps (no golden-value regression
test for `derivePartyKey`'s namespace constant, and no HTTP-route-level test
for the `/api/group*` compatibility aliases) — both are read-only findings for
a possible follow-up, not blockers: the typecheck/unit-test portion of AC5 is
green as-is.

## Visual proof (this phase)

Ran both renamed e2e proof flows against a freshly provisioned sandbox
Jellyfin stack (`./scripts/sbx.sh up -d jellyfin-sandbox` →
`sandbox-generate` → `sandbox-provision` → `up -d server client`), with a
shared `PROOF_RUN_ID=rename-group-to-party-prove`:

```
PROOF_RUN_ID=rename-group-to-party-prove ./scripts/sbx.sh run --rm -e PROOF_RUN_ID=rename-group-to-party-prove -e PROOF_FLOW=party-alias proof
PROOF_RUN_ID=rename-group-to-party-prove ./scripts/sbx.sh run --rm -e PROOF_RUN_ID=rename-group-to-party-prove -e PROOF_FLOW=party-pin proof
```

Both flows exited 0 with all internal assertions passing:
- `party-alias`: `PASS — create + alias + reuse (no duplicate)`
- `party-pin`: `PASS — invalid access token rejected`, `PASS — picker shows
  only Dave plus the "+ Add guest" card`, `PASS — add-guest modal is
  selection-only and added Carol without a PIN`, `PASS — Continue routed to
  the PIN modal; confirm disabled until PIN typed`, `PASS — wrong PIN
  rejected AT the confirm click`, `PASS — correct guest PIN formed the party
  and the app proceeded`.

Screenshots were copied from `./artifacts/rename-group-to-party-prove/...`
into this durable `.artifacts/` directory (the `./artifacts` tree does not
survive session teardown). I Read each of the following and confirmed party
terminology with no leftover "group" text in user-facing copy:

- [`party-alias-picker.png`](./party-alias-picker.png) — viewer picker
  headline reads "Pick the party".
- [`party-alias-created.png`](./party-alias-created.png) — main app header
  shows the "PARTY PICKS" recommendation rail and "Alice + Bob" active-party
  alias.
- [`party-alias-saved.png`](./party-alias-saved.png) — picker screen shows a
  "SAVED PARTIES" section listing "Alice + Bob".
- [`party-alias-reused.png`](./party-alias-reused.png) — after reselecting
  the saved party and continuing, the app returns to the same "PARTY PICKS"
  main view without creating a duplicate saved entry.
- [`party-pin-picker.png`](./party-pin-picker.png) — visitor-token picker
  shows "Pick the party" heading with only Dave plus an "Add guest" card.
- [`party-pin-prompt.png`](./party-pin-prompt.png) — "Enter guest PINs" modal
  prompting for "Carol's PIN".
- [`party-pin-wrong.png`](./party-pin-wrong.png) — wrong-PIN error text reads
  "Incorrect or missing pin for Carol."
- [`party-pin-mixed-warning.png`](./party-pin-mixed-warning.png) — "Shared
  watch progress" dialog reads "This party includes viewers beyond the
  default household set. Continuing will affect watch progress and watched
  states for ALL users in the party."
- [`party-pin-success.png`](./party-pin-success.png) — main app view after
  confirming the guest PIN, header shows "Carol" as the sole active viewer
  and the "PARTY PICKS" rail.
- [`party-pin-login.png`](./party-pin-login.png) and
  [`party-pin-invalid-token.png`](./party-pin-invalid-token.png) — the
  access-token login screen and its "Invalid access token" error; this
  screen never used "group" language (LAN household portal copy), included
  here as chain-of-custody evidence that the flow reached the picker/party
  screens through a real login round-trip rather than a stubbed session.

No screenshot showed leftover "group" wording anywhere in user-facing copy
(headings, buttons, labels, modals, or error/empty-state text).

## Verdict

**Proved.** Typecheck and unit tests are green (cited from the prior verifier
phase), and this phase adds a real, freshly-run visual proof against a live
sandbox Jellyfin showing party language end-to-end across both renamed e2e
flows, satisfying AC5's "at least one visual proof" requirement.
